<%- include('../components/auth-header', { title: 'Reset password' }) %>

<%- include('../components/auth-styles') %>
<style>
  .auth-image {
    background: url('<%= settings.loginWallpaper || assetPath('/assets/wallpapers/login.jpeg') %>') center/cover no-repeat;
  }
  .auth-link { font-size: 14px; color: var(--theme-text-link); text-align: center; display: block; margin-top: 20px; text-decoration: none; }
  .auth-link:hover { color: var(--theme-text-strong); }
</style>

<div class="auth-split">
  <div class="auth-panel" id="authPanel">

    <div class="mb-8">
      <% if (settings.logo) { %>
      <img src="<%= settings.logo %>" alt="<%= settings.title %> logo" class="h-10 w-10 rounded-xl object-contain mb-5">
      <% } %>
      <h1 class="text-2xl font-semibold" style="color:var(--theme-text-strong)"><%= t('resetPasswordTitle') %></h1>
      <p class="text-sm mt-1" style="color:var(--theme-text-muted)"><%= t('resetPasswordDesc') %></p>
    </div>

    <% if (invalidToken) { %>
    <div class="rounded-xl px-4 py-3 mb-5" style="background:var(--theme-danger-bg);border:1px solid var(--theme-danger)">
      <p class="text-sm font-medium" style="color:var(--theme-danger)">This reset link is invalid or has expired. Request a new one.</p>
    </div>
    <% } else { %>
      <% if (req.query.err === 'mismatch') { %>
      <div class="rounded-xl px-4 py-3 mb-5" style="background:var(--theme-danger-bg);border:1px solid var(--theme-danger)">
        <p class="text-sm font-medium" style="color:var(--theme-danger)">Passwords do not match.</p>
      </div>
      <% } else if (req.query.err === 'weak') { %>
      <div class="rounded-xl px-4 py-3 mb-5" style="background:var(--theme-danger-bg);border:1px solid var(--theme-danger)">
        <p class="text-sm font-medium" style="color:var(--theme-danger)">Password must be at least 8 characters with letters and numbers.</p>
      </div>
      <% } else if (req.query.err === 'expired') { %>
      <div class="rounded-xl px-4 py-3 mb-5" style="background:var(--theme-danger-bg);border:1px solid var(--theme-danger)">
        <p class="text-sm font-medium" style="color:var(--theme-danger)">This reset link is invalid or has expired. Request a new one.</p>
      </div>
      <% } else if (req.query.err === 'error') { %>
      <div class="rounded-xl px-4 py-3 mb-5" style="background:var(--theme-danger-bg);border:1px solid var(--theme-danger)">
        <p class="text-sm font-medium" style="color:var(--theme-danger)">Something went wrong. Try again.</p>
      </div>
      <% } %>

      <form id="resetForm" method="POST" action="/reset-password" autocomplete="off" novalidate>
        <%- include('../components/csrf') %>
        <input type="hidden" name="token" value="<%= token %>">
        <div class="space-y-4">
          <div>
            <label class="auth-label" for="password"><%= t('newPassword') %></label>
            <div class="pw-wrapper">
              <input id="password" name="password" type="password" autocomplete="new-password"
                required class="auth-input" placeholder="At least 8 characters" style="padding-right:40px">
              <button type="button" class="pw-toggle" id="pwToggle" aria-label="Show password">
                <span id="eyeIcon"><%- icon('eye', { width: 16, height: 16 }) %></span>
              </button>
            </div>
          </div>
          <div>
            <label class="auth-label" for="confirmPassword"><%= t('confirmPassword') %></label>
            <div class="pw-wrapper">
              <input id="confirmPassword" name="confirmPassword" type="password" autocomplete="new-password"
                required class="auth-input" placeholder="Repeat password" style="padding-right:40px">
            </div>
          </div>
          <button type="submit" class="auth-submit inline-flex items-center gap-1.5" id="submitBtn">
            <%- icon('key', { class: 'size-4', strokeWidth: 1.5 }) %>
            <span class="btn-label"><%= t('update_password') %></span>
          </button>
        </div>
      </form>
    <% } %>

    <a href="/login" class="auth-link"><%= t('backToLogin') %></a>

  </div>
  <div class="auth-image"></div>
</div>

<script nonce="<%- nonce %>">
(function () {
  requestAnimationFrame(function () {
    requestAnimationFrame(function () {
      document.getElementById('authPanel').classList.add('visible');
    });
  });

  const pwToggle = document.getElementById('pwToggle');
  const pwInput = document.getElementById('password');
  if (pwToggle && pwInput) {
    pwToggle.addEventListener('click', function () {
      const show = pwInput.type === 'password';
      pwInput.type = show ? 'text' : 'password';
      document.getElementById('eyeIcon').innerHTML = show
        ? '<path fill-rule="evenodd" d="M3.28 2.22a.75.75 0 0 0-1.06 1.06l14.5 14.5a.75.75 0 1 0 1.06-1.06l-1.745-1.745a10.029 10.029 0 0 0 3.3-4.38 1.651 1.651 0 0 0 0-1.185A10.004 10.004 0 0 0 9.999 3a9.956 9.956 0 0 0-4.744 1.194L3.28 2.22ZM7.752 6.69l1.092 1.092a2.5 2.5 0 0 1 3.374 3.373l1.091 1.092a4 4 0 0 0-5.557-5.557Z" clip-rule="evenodd"/><path d="M10.748 13.93l2.523 2.524a10.065 10.065 0 0 1-5.27 0l-1.978-1.978a4 4 0 0 0 .005-5.53l-.738-.738A8.003 8.003 0 0 0 1.834 9.587a1.651 1.651 0 0 0 0 1.186A10.004 10.004 0 0 0 10 17c1.09 0 2.14-.175 3.124-.497l-2.376-2.573Z"/>'
        : '<path d="M10 12.5a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/><path fill-rule="evenodd" d="M.664 10.59a1.651 1.651 0 0 1 0-1.186A10.004 10.004 0 0 1 10 3c4.257 0 7.893 2.66 9.336 6.41.147.381.146.804 0 1.186A10.004 10.004 0 0 1 10 17c-4.257 0-7.893-2.66-9.336-6.41ZM14 10a4 4 0 1 1-8 0 4 4 0 0 1 8 0Z" clip-rule="evenodd"/>';
  });
  }
})();
</script>

<%- include('../components/footer') %>
