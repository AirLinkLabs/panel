<%- include('../components/auth-header', { title: 'Sign in' }) %>

<%- include('../components/auth-styles') %>
<style>
  .auth-image {
    background: url('<%= settings.loginWallpaper || assetPath('/assets/wallpapers/login.jpeg') %>') center/cover no-repeat;
  }
</style>

<div class="auth-split">
  <div class="auth-panel" id="authPanel">

    <div class="mb-8">
      <% if (settings.logo) { %>
      <img src="<%= settings.logo %>" alt="<%= settings.title %> logo" class="h-10 w-10 rounded-xl object-contain mb-5">
      <% } %>
      <h1 class="text-2xl font-semibold" style="color:var(--theme-text-strong)"><%= t('loginTitle') %></h1>
      <p class="text-sm mt-1" style="color:var(--theme-text-muted)"><%= t('loginSubtitle') %> <%= settings.title %></p>
    </div>

    <% if (req.query.err) { %>
    <div id="login-error" class="rounded-xl px-4 py-3 mb-5" style="background:var(--theme-danger-bg);border:1px solid var(--theme-danger)" role="alert" aria-live="assertive">
      <p class="text-sm font-medium" style="color:var(--theme-danger)">
        <% if (req.query.err === 'missing_credentials') { %>Missing username or password.
        <% } else if (['invalid_credentials','incorrect_password','user_not_found'].includes(req.query.err)) { %>Incorrect username or password.
        <% } else if (req.query.err === 'database_error') { %>Server error — try again shortly.
        <% } else if (req.query.err === 'account_locked') { %>Account temporarily locked. Try again in <%= req.query.wait || 'a few' %> minute<%= req.query.wait == 1 ? '' : 's' %>.
        <% } else if (req.query.err === 'reset_email_sent') { %>If that email exists, a password reset link has been sent.
        <% } else if (req.query.err === 'password_reset') { %>Password updated. Sign in with your new password.
        <% } else { %>Something went wrong. Try again.<% } %>
      </p>
    </div>
    <% } %>

    <form id="loginForm" method="POST" action="/login" autocomplete="on" novalidate>
      <%- include('../components/csrf') %>
      <div class="space-y-4">
        <div>
          <label class="auth-label" for="identifier"><%= t('username') %></label>
          <input id="identifier" name="identifier" type="text" autocomplete="username"
            required spellcheck="false" autocapitalize="none"
            class="auth-input" placeholder="you@example.com"<% if (req.query.err) { %> aria-describedby="login-error" aria-invalid="true"<% } %>>
        </div>
        <div>
          <label class="auth-label" for="password"><%= t('password') %></label>
          <div class="pw-wrapper">
            <input id="password" name="password" type="password" autocomplete="current-password"
              required class="auth-input" placeholder="••••••••" style="padding-right:40px"<% if (req.query.err) { %> aria-describedby="login-error" aria-invalid="true"<% } %>>
            <button type="button" class="pw-toggle" id="pwToggle" aria-label="Show password">
              <span id="eyeIcon"><%- icon('eye', { width: 16, height: 16 }) %></span>
            </button>
          </div>
        </div>

        <label class="cb-row" id="rememberLabel">
          <input id="remember" name="remember-me" type="checkbox" class="sr-only">
          <span class="cb-box" id="cbBox">
            <span class="cb-check" id="cbCheck"><%- icon('check', { width: 10, height: 10, strokeWidth: 1.5 }) %></span>
          </span>
          <span class="text-sm" style="color:var(--theme-text-muted)"><%= t('rememberMe') %></span>
        </label>

        <div class="text-right -mt-2">
          <a href="/forgot-password" class="text-xs transition" style="color:var(--theme-text-muted)"><%= t('forgotPassword') %></a>
        </div>

        <button type="submit" class="auth-submit inline-flex items-center gap-1.5" id="submitBtn">
          <%- icon('log-in', { class: 'size-4', strokeWidth: 1.5 }) %>
          <span class="btn-label"><%= t('loginTitle') %></span>
          <span class="spinner"></span>
        </button>
      </div>
    </form>

    <% if (settings.allowRegistration) { %>
    <p class="text-sm text-center mt-6" style="color:var(--theme-text-muted)">
      <%= t('noAccount') %>
      <a href="/register" class="font-medium hover:underline" style="color:var(--theme-text-link)"><%= t('register') %></a>
    </p>
    <% } %>

  </div>
  <div class="auth-image"></div>
</div>

<script nonce="<%- nonce %>">
(function () {
  requestAnimationFrame(function () {
    requestAnimationFrame(function () {
      document.getElementById('authPanel').classList.add('visible');
    });
  });

  const pwToggle = document.getElementById('pwToggle');
  const pwInput  = document.getElementById('password');
  pwToggle.addEventListener('click', function () {
    const show = pwInput.type === 'password';
    pwInput.type = show ? 'text' : 'password';
    document.getElementById('eyeIcon').innerHTML = show
      ? '<path fill-rule="evenodd" d="M3.28 2.22a.75.75 0 0 0-1.06 1.06l14.5 14.5a.75.75 0 1 0 1.06-1.06l-1.745-1.745a10.029 10.029 0 0 0 3.3-4.38 1.651 1.651 0 0 0 0-1.185A10.004 10.004 0 0 0 9.999 3a9.956 9.956 0 0 0-4.744 1.194L3.28 2.22ZM7.752 6.69l1.092 1.092a2.5 2.5 0 0 1 3.374 3.373l1.091 1.092a4 4 0 0 0-5.557-5.557Z" clip-rule="evenodd"/><path d="M10.748 13.93l2.523 2.524a10.065 10.065 0 0 1-5.27 0l-1.978-1.978a4 4 0 0 0 .005-5.53l-.738-.738A8.003 8.003 0 0 0 1.834 9.587a1.651 1.651 0 0 0 0 1.186A10.004 10.004 0 0 0 10 17c1.09 0 2.14-.175 3.124-.497l-2.376-2.573Z"/>'
      : '<path d="M10 12.5a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/><path fill-rule="evenodd" d="M.664 10.59a1.651 1.651 0 0 1 0-1.186A10.004 10.004 0 0 1 10 3c4.257 0 7.893 2.66 9.336 6.41.147.381.146.804 0 1.186A10.004 10.004 0 0 1 10 17c-4.257 0-7.893-2.66-9.336-6.41ZM14 10a4 4 0 1 1-8 0 4 4 0 0 1 8 0Z" clip-rule="evenodd"/>';
  });

  const cbInput = document.getElementById('remember');
  const cbBox   = document.getElementById('cbBox');
  const cbCheck = document.getElementById('cbCheck');
  const accent  = getComputedStyle(document.documentElement).getPropertyValue('--theme-accent').trim();
  const accentText = getComputedStyle(document.documentElement).getPropertyValue('--theme-accent-text').trim();
  function renderCb() {
    if (cbInput.checked) {
      cbBox.style.background   = accent;
      cbBox.style.borderColor  = accent;
      cbCheck.style.color      = accentText;
      cbCheck.style.display    = 'block';
    } else {
      cbBox.style.background  = '';
      cbBox.style.borderColor = '';
      cbCheck.style.display   = 'none';
    }
  }
  document.getElementById('rememberLabel').addEventListener('click', function (e) {
    e.preventDefault();
    cbInput.checked = !cbInput.checked;
    renderCb();
  });
  // Keyboard toggle fires 'change', not 'click' — keep the visual in sync.
  cbInput.addEventListener('change', renderCb);

  document.getElementById('loginForm').addEventListener('submit', function () {
    document.getElementById('submitBtn').classList.add('loading');
    document.getElementById('submitBtn').disabled = true;
    // Animate both panels out while the POST is in flight
    document.querySelector('.auth-split').classList.add('exiting');
    document.getElementById('authPanel').classList.add('exiting');
  });
})();
</script>

<%- include('../components/footer') %>
