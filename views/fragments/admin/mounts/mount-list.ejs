<!-- Mount list fragment — replaces the card body on create/delete -->
<div id="admin-mounts-list" class="al-card">
  <h2 class="al-card-header"><%= t('allMounts') %></h2>
  <% if (mounts.length === 0) { %>
    <div class="flex flex-col items-center justify-center py-20 text-center">
      <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl mb-4" style="background:var(--theme-bg-secondary); border:1px solid var(--theme-border);">
        <%- icon('folder', { class: 'w-6 h-6', style: 'color:var(--theme-text-muted)' }) %>
      </div>
      <h3 class="text-sm font-medium" style="color:var(--theme-text-strong);"><%= t('noMountsYet') %></h3>
      <p class="text-sm mt-1 max-w-xs" style="color:var(--theme-text-muted);"><%= t('noMountsYetDesc') %></p>
      <div class="mt-5">
        <button type="button" class="al-btn-primary" hx-get="/api/v2/admin/mounts/new" hx-target="#admin-mounts-dialog" hx-swap="innerHTML">
          <span class="inline-flex items-center gap-1.5"><%- icon('plus', { class: 'w-4 h-4' }) %> <%= t('newMount') %></span>
        </button>
      </div>
    </div>
  <% } else { %>
    <div class="-mx-4 overflow-x-auto">
      <table class="al-table">
        <colgroup>
          <col style="min-width:160px">
          <col style="min-width:200px">
          <col style="min-width:200px">
          <col style="min-width:110px">
          <col style="min-width:110px">
        </colgroup>
        <thead class="al-table-head">
          <tr>
            <th scope="col" class="al-table-th sm:pl-6"><%= t('name') %></th>
            <th scope="col" class="al-table-th sm:pl-6"><%= t('mountSourceLabel') %></th>
            <th scope="col" class="al-table-th sm:pl-6"><%= t('mountTargetLabel') %></th>
            <th scope="col" class="al-table-th sm:pl-6"><%= t('mountReadOnlyLabel') %></th>
            <th scope="col" class="al-table-th sm:pl-6"><%= t('actions') %></th>
          </tr>
        </thead>
        <tbody>
          <% mounts.forEach(m => { %>
          <tr class="al-table-tr" data-mount-id="<%= m.id %>">
            <td class="al-table-td sm:pl-6">
              <div class="font-medium" style="color:var(--theme-text-strong);"><%= m.name %></div>
            </td>
            <td class="al-table-td">
              <code class="inline-block max-w-full truncate rounded-md px-2 py-1 font-mono text-xs" style="background:var(--theme-bg-secondary);color:var(--theme-text);" title="<%= m.source %>"><%= m.source %></code>
            </td>
            <td class="al-table-td">
              <code class="inline-block max-w-full truncate rounded-md px-2 py-1 font-mono text-xs" style="background:var(--theme-bg-secondary);color:var(--theme-text);" title="<%= m.target %>"><%= m.target %></code>
            </td>
            <td class="al-table-td">
              <span class="inline-flex items-center rounded-md px-2 py-1 text-xs font-medium" <%= m.readOnly ? 'style="background:var(--theme-warning-bg);color:var(--theme-warning)"' : 'style="background:var(--theme-success-bg);color:var(--theme-success)"' %>>
                <%= m.readOnly ? t('mountReadOnlyLabel') : t('mountReadWriteLabel') %>
              </span>
            </td>
            <td class="al-table-td">
              <div class="flex gap-3">
                <button
                  x-data="{ deleting: false }"
                  @click="if(!deleting && confirm('<%= (req.translations && req.translations.deleteMountConfirmList) || 'Delete this mount? Servers using it will no longer be able to attach it.' %>')) { deleting = true; fetch('/api/v2/admin/mounts/<%= m.id %>', { method: 'DELETE', headers: { 'csrf-token': document.querySelector('meta[name=csrf-token]')?.content || '' } }).then(r => { if(r.ok) { showToast('<%= (req.translations && req.translations.mountDeleted) || 'Mount deleted.', %>', 'success'); Alnav.reload(); } else { showToast('<%= (req.translations && req.translations.failedToDeleteMount) || 'Failed to delete.', %>', 'error'); deleting = false; } }).catch(() => { showToast('<%= (req.translations && req.translations.networkError) || 'Network error.', %>', 'error'); deleting = false; }); }"
                  type="button"
                  class="al-btn-ghost"
                  style="color:var(--theme-danger);"
                  :disabled="deleting"
                  aria-label="<%= t('ariaDeleteMount') %>">
                  <%- icon('trash-2', { size: 16 }) %>
                </button>
              </div>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>
</div>
