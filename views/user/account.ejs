<%- include('../components/header', { title: t('userAccountTitle') }) %>

<style>
</style>

<main class="min-h-screen">
  <div class="flex min-h-screen">
    <div class="hidden lg:block w-60 shrink-0">
      <%- include('../components/sidebar') %>
    </div>
<%- include('../components/template') %>
    <div class="flex-1 overflow-y-auto pt-0 pb-12">
      <%- include('../components/ui/breadcrumb', { items: [
        { label: t('navAccount') }
      ] }) %>

      <div class="al-page-header mb-5">
        <div class="flex items-center justify-between gap-3">
          <div class="relative shrink-0">
            <img loading="lazy" decoding="async" id="avatar-preview"
              src="<%= user.avatar ? user.avatar : '/avatar/' + encodeURIComponent(user.username) %>"
              alt="<%= t('avatar') %>"
              class="h-12 w-12 rounded-xl border object-cover" style="border-color:var(--theme-border);">
            <label for="avatar-input" title="<%= t('uploadPhoto') %>"
              class="absolute -bottom-1.5 -right-1.5 w-6 h-6 flex items-center justify-center rounded-full cursor-pointer hover:opacity-80 transition" style="background:var(--theme-text);border:2px solid var(--theme-bg);">
              <%- icon('upload', { class: 'w-3 h-3', strokeWidth: 2.5, style: 'color:var(--theme-bg);' }) %>
            </label>
            <input id="avatar-input" type="file" accept="image/*" class="hidden">
            <% if (user.avatar) { %>
            <button id="remove-avatar-btn" type="button" title="<%= t('removePhoto') %>"
              class="absolute -top-1.5 -right-1.5 w-6 h-6 flex items-center justify-center rounded-full hover:opacity-85 transition" style="background:var(--theme-danger);border:2px solid var(--theme-bg);">
              <%- icon('x', { class: 'w-3 h-3', strokeWidth: 2.5, style: 'color:var(--theme-btn-danger-text);' }) %>
            </button>
            <% } %>
          </div>
          <div class="min-w-0 flex-1">
            <h1 class="al-page-title"><%= t('userAccountTitle') %></h1>
            <p class="al-page-desc"><%= t('userAccountText') %></p>
          </div>
          <a href="/credits"
            class="al-btn-secondary text-xs">
            <%- icon('circle-alert', { class: 'w-3.5 h-3.5' }) %>
            <%= t('credits') %>
          </a>
          <%- include('../components/ui/save-button', { id: 'headerSaveBtn' }) %>
        </div>
      </div>

      <div class="al-page-body space-y-4" data-al-tabs data-tabs-default="profile" data-tabs-hash>
        <div role="tablist" aria-label="<%= t('userAccountTitle') %>" class="flex gap-1 mb-5">
          <button type="button" role="tab" data-tab="profile" aria-selected="true" tabindex="0" aria-controls="account-panel-profile"
            class="tab-btn px-4 py-2.5 text-sm font-medium transition inline-flex items-center gap-2"><%- icon('user', { class: 'size-4', strokeWidth: 1.5 }) %> <%= t('profileTab') %></button>
          <button type="button" role="tab" data-tab="images" aria-selected="false" tabindex="-1" aria-controls="account-panel-images"
            class="tab-btn px-4 py-2.5 text-sm font-medium transition inline-flex items-center gap-2"><%- icon('image', { class: 'size-4', strokeWidth: 1.5 }) %> <%= t('imagesTab') %></button>
          <button type="button" role="tab" data-tab="history" aria-selected="false" tabindex="-1" aria-controls="account-panel-history"
            class="tab-btn px-4 py-2.5 text-sm font-medium transition inline-flex items-center gap-2"><%- icon('history', { class: 'size-4', strokeWidth: 1.5 }) %> <%= t('historyTab') %></button>
        </div>

        <div role="tabpanel" data-tab-panel="profile" id="account-panel-profile">
        <div class="al-card p-4">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-4">

            <!-- Username -->
            <div>
              <label class="flex items-center gap-1.5 text-sm font-medium mb-3" style="color:var(--theme-text-strong)">
                <%- icon('user', { class: 'w-3.5 h-3.5' }) %> <%= t('username') %>
              </label>
              <div class="flex gap-1.5">
                <input id="username" type="text" name="username" placeholder="<%= user.username %>"
                  class="al-input flex-1">
                <span id="usernameFeedback" class="text-[11px] hidden" style="color:var(--theme-text-muted);"></span>
              </div>
              <div class="flex items-center gap-2 mt-1 h-4">
                <span id="usernameSaving" class="text-[11px] hidden" style="color:var(--theme-text-muted)"><%= t('saving') %></span>
                <span id="usernameSaved" class="text-[11px] hidden" style="color:var(--theme-success)"></span>
              </div>
            </div>

            <!-- Email -->
            <div>
              <label class="flex items-center gap-1.5 text-sm font-medium mb-3" style="color:var(--theme-text-strong)">
                <%- icon('mail', { class: 'w-3.5 h-3.5' }) %> <%= t('email') %>
              </label>
              <input id="email" type="email" name="email" placeholder="<%= user.email %>" class="al-input">
              <div class="flex items-center gap-2 mt-1 h-4">
                <span id="emailSaving" class="text-[11px] hidden" style="color:var(--theme-text-muted)"><%= t('saving') %></span>
                <span id="emailSaved" class="text-[11px] hidden" style="color:var(--theme-success)"></span>
              </div>
            </div>

            <!-- Current password -->
            <div class="al-field" data-al-field>
              <label for="currentPassword" class="flex items-center gap-1.5 text-sm font-medium mb-3" style="color:var(--theme-text-strong)">
                <%- icon('lock', { class: 'w-3.5 h-3.5' }) %> <%= t('currentPassword') %>
              </label>
              <div class="flex items-center gap-2">
                <div class="relative flex-1">
                  <input id="currentPassword" type="password" name="currentPassword" placeholder="<%= req.translations.currentPassword || 'Current password' %>" class="al-input pr-10" data-al-al-input autocomplete="current-password">
                  <button type="button" data-al-field-toggle aria-label="<%= t('showPassword') %>" aria-pressed="false" class="al-btn-ghost absolute right-1 top-1/2 -translate-y-1/2 h-8 w-8">
                    <%- icon('eye', { class: 'w-4 h-4' }) %>
                  </button>
                </div>
              </div>
              <span id="current-password-feedback" class="text-[11px] block mt-1.5 hidden" style="color:var(--theme-text-muted);"></span>
              <p class="al-field-message" data-al-field-message id="current-password-message" role="alert" hidden></p>
            </div>

            <!-- New password -->
            <div class="al-field" data-al-field>
              <label for="newPassword" class="flex items-center gap-1.5 text-sm font-medium mb-3" style="color:var(--theme-text-strong)">
                <%- icon('lock', { class: 'w-3.5 h-3.5' }) %> <%= t('newPassword') %>
              </label>
              <div class="relative">
                <input id="newPassword" type="password" name="newPassword" placeholder="<%= req.translations.newPassword || 'New password' %>" disabled class="al-input pr-10 disabled:opacity-50" data-al-al-input autocomplete="new-password">
                <button type="button" data-al-field-toggle aria-label="<%= t('showPassword') %>" aria-pressed="false" class="al-btn-ghost absolute right-1 top-1/2 -translate-y-1/2 h-8 w-8">
                  <%- icon('eye', { class: 'w-4 h-4' }) %>
                </button>
              </div>
              <p class="al-field-message" data-al-field-message id="new-password-message" role="alert" hidden></p>
            </div>

            <!-- Description (full width) -->
            <div class="md:col-span-2">
              <label class="flex items-center gap-1.5 text-sm font-medium mb-3" style="color:var(--theme-text-strong)">
                <%- icon('file-text', { class: 'w-3.5 h-3.5' }) %> <%= t('description') %>
              </label>
              <textarea id="description" name="description" rows="2" class="al-input resize-none"><%= user.description %></textarea>
              <div class="flex items-center gap-2 mt-1 h-4">
                <span id="descriptionSaving" class="text-[11px] hidden" style="color:var(--theme-text-muted)"><%= t('saving') %></span>
                <span id="descriptionSaved" class="text-[11px] hidden" style="color:var(--theme-success)"></span>
              </div>
            </div>

            <!-- Language -->
            <div>
              <label for="language" class="flex items-center gap-1.5 text-sm font-medium mb-3" style="color:var(--theme-text-strong)">
                <%- icon('globe', { class: 'w-3.5 h-3.5' }) %> <%= t('languagePreference') %>
              </label>
              <select id="language" name="language" class="cs-native">
                <% const languageNames = { en: 'English', fr: 'Français', de: 'Deutsch', es: 'Español', pt: 'Português', it: 'Italiano', ru: 'Русский', zh: '中文', ja: '日本語', ta: 'தமிழ்' }; %>
                <% (SUPPORTED_LANGUAGES || ['en']).forEach(function(lang) { %>
                <option value="<%= lang %>" <%= req.lang === lang ? 'selected' : '' %>><%= languageNames[lang] || lang %></option>
                <% }); %>
              </select>
              <div class="custom-select" data-for="language"></div>
              <div class="flex items-center gap-2 mt-1 h-4">
                <span id="languageSaving" class="text-[11px] hidden" style="color:var(--theme-text-muted)"><%= t('saving') %></span>
                <span id="languageSaved" class="text-[11px] hidden" style="color:var(--theme-success)"></span>
              </div>
            </div>

            <!-- 2FA -->
            <div>
              <p class="flex items-center gap-1.5 text-sm font-medium mb-3" style="color:var(--theme-text-strong)">
                <%- icon('shield', { class: 'w-3.5 h-3.5' }) %> <%= t('twoFactorAuth') %>
              </p>
              <div id="twofa-status">
              <% if (user.totpEnabled !== true) { %>
              <p class="text-sm mb-3" style="color:var(--theme-text-muted)"><%= t('addExtraSecurity') %></p>
              <a href="/account/2fa/setup" class="al-btn-primary text-sm inline-flex"><%= t('setup2FA') %></a>
              <% } else { %>
              <div class="flex items-center gap-2 mb-3">
                <span class="inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium"
                  style="background:var(--theme-success-bg);color:var(--theme-success)">
                  <%- icon('check', { class: 'w-3 h-3' }) %> <%= t('enabled') %>
                </span>
              </div>
              <div class="flex gap-2 items-center">
                <input id="disable2faPassword" type="password" placeholder="<%= t('currentPassword') %>" class="al-input text-sm flex-1" style="max-width:180px">
                <button id="disable2faBtn" type="button" class="al-btn-secondary text-sm"><%- icon('shield-off', { class: 'size-4', strokeWidth: 1.5 }) %><%= t('disable2FA') %></button>
              </div>
              <% } %>
              <p class="text-xs mt-2" style="color:var(--theme-text-muted)">
                <span id="passkey-count-info"></span>
                <a href="/account/2fa/setup" style="color:var(--theme-text-link)" class="hover:underline"><%= t('managePasskeys') %></a>
              </p>
              </div>
            </div>

          </div>

          <!-- Universal save button — triggers saves on all dirty fields -->
          <div class="mt-6 pt-5" style="border-top:1px solid var(--theme-border)">
            <button id="saveAccountBtn" type="button" class="al-btn-primary px-6 py-2.5 text-sm font-medium">
              <span id="saveBtnText"><%= t('saveChanges') %></span>
              <span id="saveBtnSaving" class="hidden"><%= t('saving') %></span>
            </button>
          </div>
        </div>

        <!-- Preferred node -->
        <div class="al-card p-4">
          <p class="text-sm font-medium mb-1" style="color:var(--theme-text-strong)"><%= t('preferredNode') %></p>
          <p class="text-xs mb-4" style="color:var(--theme-text-muted)"><%= t('preferredNodeDesc') %></p>
          <select id="preferredNode" class="al-input">
            <option value=""><%= req.translations.autoSelectNode || 'Auto (least loaded)' %></option>
            <% if (nodes && nodes.length) { nodes.forEach(node => { %>
            <option value="<%= node.id %>"<%= user?.preferredNodeId === node.id ? ' selected' : '' %>><%= node.name %> — <%= node.address %></option>
            <% }); } %>
          </select>
          <div class="flex items-center gap-2 mt-2 h-4">
            <span id="preferredNodeSaving" class="text-[11px] hidden" style="color:var(--theme-text-muted)"><%= t('saving') %></span>
            <span id="preferredNodeSaved" class="text-[11px] hidden" style="color:var(--theme-success)"></span>
          </div>
        </div>
        </div>

        <div role="tabpanel" data-tab-panel="images" id="account-panel-images" hidden>
        <!-- Custom image submissions — full workspace -->
        <div class="al-card p-4" id="customImagesSection">
          <div class="flex flex-wrap items-start justify-between gap-3 mb-4">
            <div>
              <p class="flex items-center gap-1.5 text-sm font-medium mb-1" style="color:var(--theme-text-strong)">
                <%- icon('image', { class: 'w-4 h-4', strokeWidth: 1.5 }) %> <%= t('customImages') %>
              </p>
              <p class="text-xs" style="color:var(--theme-text-muted)"><%= t('customImagesDesc') %></p>
            </div>
            <% if (allowed) { %>
            <button id="openSubmitBtn" type="button" class="al-btn-primary px-4 py-2 text-sm font-medium inline-flex items-center gap-1.5 shrink-0">
              <%- icon('plus', { class: 'size-4', strokeWidth: 1.5 }) %>
              <%= t('submitImage') %>
            </button>
            <% } %>
          </div>

          <% if (!allowed) { %>
          <p class="text-xs" style="color:var(--theme-text-muted)"><%= t('imageSubmissionsDisabledText') %></p>
          <% } else { %>
            <!-- Inline image list (replaces /my-images standalone page) -->
            <div id="myImagesEmpty" class="flex flex-col items-center justify-center py-12 text-center <%= (!images || images.length === 0) ? '' : 'hidden' %>">
              <div class="w-10 h-10 rounded-full flex items-center justify-center mb-3" style="background:var(--theme-bg-secondary)">
                <%- icon('image', { class: 'w-5 h-5', style: 'color:var(--theme-text-muted)' }) %>
              </div>
              <p class="text-sm font-medium" style="color:var(--theme-text-strong)">
                <%= t('noSubmittedImages') %>
              </p>
              <p class="text-xs mt-1" style="color:var(--theme-text-muted)">
                <%= t('noSubmittedImagesText') %>
              </p>
            </div>
            <div id="myImagesTable" class="overflow-x-auto shadow-sm rounded-xl border <%= (!images || images.length === 0) ? 'hidden' : '' %>" style="border-color:var(--theme-border)">
              <table class="al-table min-w-full">
                <thead class="al-table-head">
                  <tr>
                    <th class="al-table-th py-3 pl-6 pr-3"><%= t('name') %></th>
                    <th class="al-table-th px-3 py-3"><%= t('submitted') %></th>
                    <th class="al-table-th px-3 py-3"><%= t('statusColumn') %></th>
                    <th class="al-table-th px-3 py-3"><%= t('actions') %></th>
                  </tr>
                </thead>
                <tbody class="divide-y" style="border-color:var(--theme-border)">
                  <% let imgIdx = 0; (images || []).forEach(function(image) { %>
                  <tr class="al-table-tr transition-colors" style="--i:<%= imgIdx++ %>" data-image-id="<%= image.id %>">
                    <td class="al-table-td whitespace-nowrap py-3 pl-6 pr-3 font-medium" style="color:var(--theme-text-strong)">
                      <%= image.name %>
                      <% if (image.status === 'rejected' && image.rejectionReason) { %>
                      <p class="text-xs mt-0.5" style="color:var(--theme-danger)"><%= image.rejectionReason %></p>
                      <% } %>
                    </td>
                    <td class="al-table-td whitespace-nowrap px-3 py-3"><%= new Date(image.createdAt).toLocaleDateString() %></td>
                    <td class="al-table-td whitespace-nowrap px-3 py-3">
                      <% if (image.status === 'approved') { %>
                      <span class="inline-flex items-center rounded-md px-2 py-1 text-xs font-medium ring-1 ring-inset"
                        style="background:var(--theme-success-bg);color:var(--theme-success)">
                        <%= t('approved') %>
                      </span>
                      <% } else if (image.status === 'rejected') { %>
                      <span class="inline-flex items-center rounded-md px-2 py-1 text-xs font-medium ring-1 ring-inset"
                        style="background:var(--theme-danger-bg);color:var(--theme-danger)">
                        <%= t('rejected') %>
                      </span>
                      <% } else { %>
                      <span class="inline-flex items-center rounded-md px-2 py-1 text-xs font-medium ring-1 ring-inset"
                        style="background:var(--theme-warning-bg);color:var(--theme-warning)">
                        <%= t('pendingApproval') %>
                      </span>
                      <% } %>
                    </td>
                    <td class="whitespace-nowrap px-3 py-3 text-sm">
                      <div class="flex gap-2">
                        <% if (image.status !== 'approved') { %>
                        <a href="/my-images/edit/<%= image.id %>" class="al-btn-secondary px-3 py-2 text-sm font-medium inline-flex items-center gap-1.5">
                          <%- icon('pencil', { class: 'size-4', strokeWidth: 1.5 }) %><%= t('update') %>
                        </a>
                        <% } %>
                        <button type="button" class="al-btn-danger" data-delete-image="<%= image.id %>" aria-label="Delete <%= image.name %>">
                          <%- icon('trash-2', { size: 16, class: 'shrink-0' }) %>
                        </button>
                      </div>
                    </td>
                  </tr>
                  <% }); %>
                </tbody>
              </table>
            </div>
          <% } %>
        </div>
        </div>

        <div role="tabpanel" data-tab-panel="history" id="account-panel-history" hidden>
        <!-- Login history -->
        <div class="al-card p-4">
          <p class="text-sm font-medium mb-4" style="color:var(--theme-text-strong)"><%= t('loginHistory') %></p>
          <div class="overflow-x-auto shadow-sm rounded-xl border" style="border-color:var(--theme-border)">
            <table class="al-table min-w-full text-sm">
              <colgroup>
                <col style="min-width:200px">
                <col style="min-width:140px">
                <col style="min-width:280px">
              </colgroup>
              <thead class="al-table-head">
                <tr>
                  <th class="al-table-th px-4 py-3"><%= t('dateTime') %></th>
                  <th class="al-table-th px-4 py-3"><%= t('ipAddress') %></th>
                  <th class="al-table-th px-4 py-3"><%= t('browserDevice') %></th>
                </tr>
              </thead>
              <tbody class="divide-y" style="border-color:var(--theme-border)">
                <% if (loginHistory && loginHistory.length > 0) { %>
                  <% let loginIdx = 0; loginHistory.forEach(login => { %>
                  <tr style="--i:<%= loginIdx++ %>" class="al-table-tr transition-colors">
                    <td class="px-4 py-3 whitespace-nowrap" style="color:var(--theme-text);"><%= new Date(login.timestamp).toLocaleString() %></td>
                    <td class="px-4 py-3 whitespace-nowrap font-mono text-xs" style="color:var(--theme-text-muted);"><%= login.ipAddress || t('unknown') %></td>
                    <td class="px-4 py-3 text-xs truncate max-w-[260px]" style="color:var(--theme-text-muted);"><%= login.userAgent || t('unknown') %></td>
                  </tr>
                  <% }) %>
                <% } else { %>
                <tr><td colspan="3" class="px-4 py-6 text-center text-sm" style="color:var(--theme-text-muted);"><%= t('noLoginHistory') %></td></tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
        </div>

      </div>
</div>
  </div>
</main>

<div id="image-submit-data" style="display:none"
  data-submitting="<%= t('submittingImage') %>"
  data-submit-error="<%= t('failedToSubmitImage') %>"
></div>

<!-- Image submission popup -->
<% if (allowed) { %>
<dialog id="imModal" class="al-dialog rounded-2xl max-w-lg w-full shadow-xl max-h-[88vh] p-0" style="background:var(--theme-bg-card); border:1px solid var(--theme-border);">
  <div class="flex flex-col max-h-[88vh]">
    <div class="al-sheet-header" style="border-color:var(--theme-border)">
      <div>
        <h2 class="text-sm font-semibold" style="color:var(--theme-text-strong)"><%= t('submitImage') %></h2>
        <p class="text-xs mt-0.5" style="color:var(--theme-text-muted)"><%= t('submitImageText') %></p>
      </div>
      <button type="button" id="imClose" class="al-btn-ghost p-2 rounded-lg min-w-[44px] min-h-[44px] flex items-center justify-center shrink-0" aria-label="<%= t('close') %>">
        <%- icon('x', { class: 'h-5 w-5', strokeWidth: 1.5 }) %>
      </button>
    </div>
    <div class="flex-1 min-h-0 overflow-y-auto px-6 py-4">
      <div class="flex gap-1 p-1 rounded-xl mb-4" style="background:var(--theme-bg-secondary)">
        <button type="button" id="imModeManual" class="flex-1 px-3 py-2 text-xs font-medium rounded-lg transition" style="color:var(--theme-text-strong); background:var(--theme-bg-card);"><%= t('manualEnter') %></button>
        <button type="button" id="imModeJson" class="flex-1 px-3 py-2 text-xs font-medium rounded-lg transition" style="color:var(--theme-text-muted);"><%= t('pasteJson') %></button>
        <button type="button" id="imModeUrl" class="flex-1 px-3 py-2 text-xs font-medium rounded-lg transition" style="color:var(--theme-text-muted);"><%= t('importFromUrl') %></button>
      </div>

      <!-- Manual form -->
      <form id="imManualForm" class="flex flex-col gap-4">
        <div class="grid grid-cols-2 gap-3">
          <div class="col-span-2"><label class="al-label"><%= t('imageFormName') %> <span style="color:var(--theme-danger)">*</span></label><input class="al-input" type="text" name="name" required placeholder="e.g. My Custom Game"></div>
          <div><label class="al-label"><%= t('imageFormAuthor') %></label><input class="al-input" type="text" name="author" placeholder="e.g. github.com/you"></div>
          <div><label class="al-label"><%= t('imageFormAuthorName') %></label><input class="al-input" type="text" name="authorName" placeholder="Your Name"></div>
          <div class="col-span-2"><label class="al-label"><%= t('imageFormStartupCommand') %> <span style="color:var(--theme-danger)">*</span></label><input class="al-input" type="text" name="startup" required placeholder="java -Xms128M -jar server.jar" style="font-family:ui-monospace,monospace;font-size:12px;"></div>
          <div class="col-span-2"><label class="al-label"><%= t('imageFormDockerImages') %> <span style="color:var(--theme-danger)">*</span></label><input class="al-input" type="text" name="dockerImages" required placeholder='[{"game":"ghcr.io/you/image:latest"}]' style="font-family:ui-monospace,monospace;font-size:12px;"></div>
          <div class="col-span-2"><label class="al-label"><%= t('imageFormDescription') %></label><textarea class="al-input" name="description" rows="2" placeholder="What does this image run?" style="resize:none;"></textarea></div>
          <div class="col-span-2"><label class="al-label"><%= t('imageFormVariables') %> <span class="font-normal" style="color:var(--theme-text-muted)"><%= t('variablesHint') %></span></label><textarea class="al-input" name="variables" rows="4" placeholder="[]" style="resize:none;font-family:ui-monospace,monospace;font-size:12px;">[]</textarea></div>
        </div>
        <div class="flex gap-2 pt-1">
          <button type="button" id="imManualCancel" class="al-btn-secondary flex-1 py-2 inline-flex items-center justify-center gap-1.5"><%- icon('x', { class: 'size-4', strokeWidth: 1.5 }) %><%= t('cancel') %></button>
          <button type="submit" class="al-btn-primary flex-1 py-2 inline-flex items-center justify-center gap-1.5"><%- icon('send', { class: 'size-4', strokeWidth: 1.5 }) %><%= t('submitForReview') %></button>
        </div>
      </form>

      <!-- JSON paste -->
      <div id="imJsonSection" class="hidden">
        <label class="al-label" for="imJsonInput"><%= t('eggJson') %></label>
        <textarea id="imJsonInput" class="al-input" rows="12" placeholder='{ "name": "...", "startup": "...", "docker_images": { "game": "ghcr.io/example/image:latest" } }' style="resize:none;font-family:ui-monospace,monospace;font-size:12px;"></textarea>
        <div class="flex gap-2 pt-4">
          <button type="button" id="imJsonCancel" class="al-btn-secondary flex-1 py-2 inline-flex items-center justify-center gap-1.5"><%= t('cancel') %></button>
          <button type="button" id="imSubmitJsonBtn" class="al-btn-primary flex-1 py-2 inline-flex items-center justify-center gap-1.5"><%- icon('send', { class: 'size-4', strokeWidth: 1.5 }) %><%= t('submitForReview') %></button>
        </div>
      </div>

      <!-- URL import -->
      <div id="imUrlSection" class="hidden">
        <label class="al-label" for="imUrlInput"><%= t('imageUrlLabel') %></label>
        <input id="imUrlInput" type="url" class="al-input" placeholder="<%= t('imageUrlPlaceholder') %>">
        <p class="mt-2 text-xs" style="color:var(--theme-text-muted)"><%= t('fetchUrlDesc') %></p>
        <div class="flex gap-2 pt-4">
          <button type="button" id="imUrlCancel" class="al-btn-secondary flex-1 py-2 inline-flex items-center justify-center gap-1.5"><%= t('cancel') %></button>
          <button type="button" id="imSubmitUrlBtn" class="al-btn-primary flex-1 py-2 inline-flex items-center justify-center gap-1.5"><%- icon('download', { class: 'size-4', strokeWidth: 1.5 }) %><%= t('fetchAndSubmit') %></button>
        </div>
      </div>
    </div>
  </div>
</dialog>
<% } %>

<script nonce="<%- nonce %>">
  const _toastSys = (typeof createToastSystem === 'function') ? createToastSystem() : { showToast: (typeof window.showToast === 'function') ? window.showToast : function(){} };
  const { showToast } = _toastSys;
  const csrfToken = document.querySelector('meta[name="csrf-token"]')?.getAttribute('content');

  // ── Auto-save helper for each field ──────────────────────────────
  // Called by vanilla JS handlers on each field
  window._accountSave = async function(field) {
    let url, data;
    switch (field) {
      case 'username':
        data = { newUsername: document.getElementById('username')?.value?.trim() };
        if (!data.newUsername) return null;
        url = '/api/v2/account/update-username';
        break;
      case 'email':
        data = { email: document.getElementById('email')?.value?.trim() };
        if (!data.email) return null;
        url = '/api/v2/account/change-email';
        break;
      case 'description':
        data = { description: document.getElementById('description')?.value?.trim() || '' };
        url = '/api/v2/account/update-description';
        break;
      case 'language':
        data = { language: document.getElementById('language')?.value };
        if (!data.language) return null;
        url = '/api/v2/account/set-language';
        break;
      case 'preferredNode': {
        const nodeId = document.getElementById('preferredNode')?.value;
        data = { preferredNodeId: nodeId || null };
        url = '/api/v2/account/set-preferred-node';
        break;
      }
      case 'password': {
        const cpw = document.getElementById('currentPassword')?.value;
        const npw = document.getElementById('newPassword')?.value;
        if (!npw || !cpw || document.getElementById('newPassword')?.disabled) return null;
        data = { currentPassword: cpw, newPassword: npw };
        url = '/api/v2/account/change-password';
        break;
      }
      default: return null;
    }
    try {
      const res = await fetch(url, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'CSRF-Token': csrfToken },
        body: JSON.stringify(data),
      });
      const result = await res.json();
      if (!res.ok) {
        showToast(result.error || result.message || '<%= req.translations.failedToSaveField || "Failed to save" %> ' + field, 'error');
        return null;
      }
      // Update sidebar username if changed
      if (field === 'username') {
        const sb = document.getElementById('sidebar-username');
        if (sb) sb.textContent = data.newUsername;
      }
      // Clear password fields after successful change
      if (field === 'password') {
        const cpwEl = document.getElementById('currentPassword');
        const npwEl = document.getElementById('newPassword');
        const pwFb = document.getElementById('current-password-feedback');
        if (cpwEl) cpwEl.value = '';
        if (npwEl) { npwEl.value = ''; npwEl.disabled = true; }
        if (pwFb) pwFb.textContent = '';
      }
      return result;
    } catch (err) {
      showToast('<%= req.translations.failedToSaveField || "Failed to save" %> ' + field, 'error');
      return null;
    }
  };

  // ── Username availability check ──────────────────────────────────
  const usernameInput = document.getElementById('username');
  const usernameFb = document.getElementById('usernameFeedback');
  let usernameTimer;
  usernameInput?.addEventListener('input', () => {
    clearTimeout(usernameTimer);
    usernameTimer = setTimeout(async () => {
      const val = usernameInput.value.trim();
      if (!val || !usernameFb) return;
      try {
        const res = await fetch(`/api/v2/account/check-username?username=${encodeURIComponent(val)}`);
        const { exists } = await res.json();
        usernameFb.textContent = exists ? '<%= req.translations.usernameTaken || "Username taken" %>' : '<%= req.translations.usernameIsNotTaken || "Username available" %>';
        usernameFb.style.color = exists ? 'var(--theme-danger)' : 'var(--theme-success)';
        usernameFb.classList.remove('hidden');
      } catch (err) { console.error('Username check failed:', err); }
    }, 500);
  });

  // ── Password validation ──────────────────────────────────────────
  const currentPwInput = document.getElementById('currentPassword');
  const newPwInput = document.getElementById('newPassword');
  const pwFeedback = document.getElementById('current-password-feedback');
  let pwTimer;
  currentPwInput?.addEventListener('input', () => {
    clearTimeout(pwTimer);
    const val = currentPwInput.value.trim();
    if (!val) { if (pwFeedback) pwFeedback.textContent = ''; newPwInput.disabled = true; newPwInput.value = ''; return; }
    pwTimer = setTimeout(async () => {
      try {
        const res = await fetch('/api/v2/account/validate-password', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'CSRF-Token': csrfToken },
          body: JSON.stringify({ currentPassword: val }),
        });
        const { valid } = await res.json();
        if (pwFeedback) pwFeedback.textContent = valid ? '<%= req.translations.correctPassword || "Correct" %>' : '<%= req.translations.incorrectPassword || "Incorrect password" %>';
        newPwInput.disabled = !valid;
      } catch (err) { console.error('Password validation failed:', err); if (pwFeedback) pwFeedback.textContent = 'Could not validate'; newPwInput.disabled = true; }
    }, 500);
  });

  // ── Auto-save: debounced field handlers ─────────────────────────
  function showFieldSaving(id) {
    var s = document.getElementById(id + 'Saving');
    var d = document.getElementById(id + 'Saved');
    if (s) s.classList.remove('hidden');
    if (d) d.classList.add('hidden');
  }
  function showFieldSaved(id) {
    var s = document.getElementById(id + 'Saving');
    var d = document.getElementById(id + 'Saved');
    if (s) s.classList.add('hidden');
    if (d) {
      d.textContent = 'Saved ' + new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'});
      d.classList.remove('hidden');
    }
  }
  function debounceField(field, inputId, eventType) {
    var el = document.getElementById(inputId);
    if (!el) return;
    var timer;
    el.addEventListener(eventType || 'input', function () {
      clearTimeout(timer);
      showFieldSaving(field);
      timer = setTimeout(async function () {
        var result = await window._accountSave(field);
        if (result !== null) showFieldSaved(field);
      }, 600);
    });
  }
  debounceField('username', 'username', 'input');
  debounceField('email', 'email', 'input');
  debounceField('description', 'description', 'input');
  ['language', 'preferredNode'].forEach(function (field) {
    var el = document.getElementById(field);
    if (!el) return;
    el.addEventListener('change', function () {
      showFieldSaving(field);
      window._accountSave(field).then(function (d) {
        if (d !== null) showFieldSaved(field);
      });
    });
  });

  // ── Save-all buttons (header + profile panel) ──────────────────
  async function saveAllFields(btn) {
    var origDisabled = btn.disabled;
    btn.disabled = true;
    try {
      var results = await Promise.all(['username','email','password','description','language','preferredNode'].map(function (f) { return window._accountSave(f); }));
      if (results.some(function (d) { return d !== null; })) showToast('All changes saved.', 'success');
    } finally {
      btn.disabled = origDisabled;
    }
  }
  document.getElementById('saveAccountBtn')?.addEventListener('click', function () { saveAllFields(this); });
  document.getElementById('headerSaveBtn')?.addEventListener('click', function () { saveAllFields(this); });

  // Avatar — open upload modal, then crop
  const avatarPreview = document.getElementById('avatar-preview');
  document.querySelector('label[for="avatar-input"]')?.addEventListener('click', (e) => {
    e.preventDefault();
    openUploadModal({
      title: '<%= req.translations.uploadProfilePhoto || "Upload profile photo" %>',
      accept: 'image/*',
      onFile: async (file) => {
        const MAX_AVATAR_SIZE = 2 * 1024 * 1024;
        if (file.size > MAX_AVATAR_SIZE) { showToast('<%= req.translations.imageMustBeUnder2mb || "Image must be under 2 MB" %>', 'error'); return; }
        const reader = new FileReader();
        reader.onload = evt => openCropPopup(evt.target.result, csrfToken);
        reader.readAsDataURL(file);
      }
    });
  });

  document.getElementById('remove-avatar-btn')?.addEventListener('click', async () => {
    const res = await fetch('/api/v2/account/avatar', { method: 'DELETE', headers: { 'Content-Type': 'application/json', 'CSRF-Token': csrfToken } });
    if (res.ok) {
      avatarPreview.src = '/avatar/<%= encodeURIComponent(user.username) %>';
      const removeAvatarBtn = document.getElementById('remove-avatar-btn');
      if (removeAvatarBtn) removeAvatarBtn.style.display = 'none';
      showToast('<%= req.translations.profilePictureRemoved || "Profile picture removed." %>', 'success');
    } else showToast('<%= req.translations.errorOccurred || "Something went wrong." %>', 'error');
  });

  // Disable 2FA (requires current password)
  document.getElementById('disable2faBtn')?.addEventListener('click', async () => {
    const password = document.getElementById('disable2faPassword')?.value || '';
    if (!password) { showToast('<%= req.translations.enterPasswordToDisable2fa || "Enter your current password to disable 2FA." %>', 'error'); return; }
    const btn = document.getElementById('disable2faBtn');
    btn.disabled = true;
    try {
      const res = await fetch('/api/v2/account/2fa/disable', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'CSRF-Token': csrfToken },
        body: JSON.stringify({ password }),
      });
      const data = await res.json();
      if (res.ok && data.success) {
        showToast('<%= req.translations.twoFactorDisabled || "Two-factor authentication disabled." %>', 'success');
        const twofaStatus = document.getElementById('twofa-status');
        window.location.reload();
      } else {
        showToast(data.error || '<%= req.translations.failedToDisable2fa || "Failed to disable 2FA." %>', 'error');
      }
    } catch (err) {
      console.error('Failed to disable 2FA:', err);
      showToast('<%= req.translations.errorOccurred || "Something went wrong." %>', 'error');
    }
    btn.disabled = false;
  });

  // Load passkey count
  (async function () {
    try {
      const res = await fetch('/api/v2/account/passkey/list');
      if (!res.ok) return;
      const data = await res.json();
      const passkeys = data.passkeys || [];
      const el = document.getElementById('passkey-count-info');
      if (el && passkeys.length > 0) {
        el.textContent = passkeys.length + ' <%= req.translations.passkeyCount || "passkey" %>' + (passkeys.length !== 1 ? 's' : '') + ' <%= req.translations.registered || "registered" %> · ';
      }
    } catch (e) {}
  })();

  function openCropPopup(dataUrl, csrf) {
    const overlay = document.createElement('div');
    overlay.className = 'al-sheet-overlay fixed inset-0 z-50 flex items-center justify-center p-4';
    overlay.innerHTML = `
      <div class="al-sheet-panel rounded-2xl shadow-lg border w-full max-w-lg overflow-hidden" style="background:var(--theme-bg-card);border-color:var(--theme-border);">
        <div class="flex items-center justify-between px-5 pt-5 pb-3 border-b" style="border-color:var(--theme-border);">
          <div>
            <p class="text-sm font-semibold" style="color:var(--theme-text-strong);"><%= req.translations.positionYourPhoto || "Position your photo" %></p>
            <p class="text-xs mt-0.5" style="color:var(--theme-text-muted);"><%= req.translations.dragResizeSelection || "Drag and resize the selection box." %></p>
          </div>
          <button type="button" id="crop-cancel" class="w-9 h-9 flex items-center justify-center rounded-lg hover:opacity-75 transition" style="color:var(--theme-text-muted);">
            <%- icon('x', { class: 'w-4 h-4', strokeWidth: 2 }) %>
          </button>
        </div>
        <div class="relative select-none overflow-hidden" style="height:340px;background:var(--theme-bg);" id="crop-stage">
          <img id="crop-img" src="${dataUrl}" draggable="false" alt="<%= t('cropPreview') %>" style="position:absolute;top:0;left:0;user-select:none;max-width:none"/>
          <div id="crop-box" style="position:absolute;border:2px solid white;box-shadow:0 0 0 9999px rgba(0,0,0,0.55);cursor:move;box-sizing:border-box">
            <div class="crop-handle" data-dir="nw" style="position:absolute;top:-5px;left:-5px;width:10px;height:10px;background:white;border-radius:50%;cursor:nw-resize"></div>
            <div class="crop-handle" data-dir="ne" style="position:absolute;top:-5px;right:-5px;width:10px;height:10px;background:white;border-radius:50%;cursor:ne-resize"></div>
            <div class="crop-handle" data-dir="sw" style="position:absolute;bottom:-5px;left:-5px;width:10px;height:10px;background:white;border-radius:50%;cursor:sw-resize"></div>
            <div class="crop-handle" data-dir="se" style="position:absolute;bottom:-5px;right:-5px;width:10px;height:10px;background:white;border-radius:50%;cursor:se-resize"></div>
          </div>
        </div>
        <div class="flex gap-3 px-5 py-4 border-t" style="border-color:var(--theme-border);">
          <button type="button" id="crop-cancel-btn" class="al-btn-secondary flex-1 py-2.5"><%= t('cancel') %></button>
          <button type="button" id="crop-save" class="al-btn-primary flex-1 py-2.5"><%= t('savePhoto') %></button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    var _p = overlay.querySelector('.al-sheet-panel');
    overlay.classList.add('open'); if (_p) _p.classList.add('open');

    const stage = document.getElementById('crop-stage');
    const img   = document.getElementById('crop-img');
    const box   = document.getElementById('crop-box');
    const stageW = stage.offsetWidth, stageH = stage.offsetHeight;

    img.onload = () => {
      const scale = Math.min(stageW / img.naturalWidth, stageH / img.naturalHeight);
      const dw = img.naturalWidth * scale, dh = img.naturalHeight * scale;
      img.style.width = dw + 'px'; img.style.height = dh + 'px';
      img.style.left = ((stageW - dw) / 2) + 'px'; img.style.top = ((stageH - dh) / 2) + 'px';
      img._scale = scale; img._offX = (stageW - dw) / 2; img._offY = (stageH - dh) / 2;
      const bs = Math.min(dw, dh, 200);
      box.style.width = bs + 'px'; box.style.height = bs + 'px';
      box.style.left = ((stageW - bs) / 2) + 'px'; box.style.top = ((stageH - bs) / 2) + 'px';
    };
    if (img.complete) img.onload();

    let action = null, sx, sy, sb;
    const clamp = (v, a, b) => Math.max(a, Math.min(b, v));

    box.addEventListener('mousedown', e => {
      if (e.target.classList.contains('crop-handle')) return;
      e.preventDefault(); action = 'move'; sx = e.clientX; sy = e.clientY;
      sb = { l: parseInt(box.style.left), t: parseInt(box.style.top), w: box.offsetWidth, h: box.offsetHeight };
    });
    box.querySelectorAll('.crop-handle').forEach(h => h.addEventListener('mousedown', e => {
      e.preventDefault(); e.stopPropagation(); action = h.dataset.dir; sx = e.clientX; sy = e.clientY;
      sb = { l: parseInt(box.style.left), t: parseInt(box.style.top), w: box.offsetWidth, h: box.offsetHeight };
    }));

    const onUp = () => action = null;
    const onMove = e => {
      if (!action) return;
      const dx = e.clientX - sx, dy = e.clientY - sy, min = 60;
      if (action === 'move') {
        box.style.left = clamp(sb.l + dx, 0, stageW - sb.w) + 'px';
        box.style.top  = clamp(sb.t + dy, 0, stageH - sb.h) + 'px';
      } else {
        let l = sb.l, t = sb.t, w = sb.w, h = sb.h;
        if (action.includes('e')) w = Math.max(min, sb.w + dx);
        if (action.includes('s')) h = Math.max(min, sb.h + dy);
        if (action.includes('w')) { const nw = Math.max(min, sb.w - dx); l = sb.l + (sb.w - nw); w = nw; }
        if (action.includes('n')) { const nh = Math.max(min, sb.h - dy); t = sb.t + (sb.h - nh); h = nh; }
        l = clamp(l, 0, stageW - min); t = clamp(t, 0, stageH - min);
        w = Math.min(w, stageW - l); h = Math.min(h, stageH - t);
        box.style.left = l + 'px'; box.style.top = t + 'px';
        box.style.width = w + 'px'; box.style.height = h + 'px';
      }
    };

    document.addEventListener('mousemove', onMove);
    document.addEventListener('mouseup', onUp);

    const close = () => {
      document.removeEventListener('mousemove', onMove);
      document.removeEventListener('mouseup', onUp);
      var _p = overlay.querySelector('.al-sheet-panel'); overlay.classList.remove('open'); if (_p) _p.classList.remove('open'); overlay.remove();
    };
    document.getElementById('crop-cancel').addEventListener('click', close);
    document.getElementById('crop-cancel-btn').addEventListener('click', close);

    document.getElementById('crop-save').addEventListener('click', () => {
      const scale = img._scale, offX = img._offX, offY = img._offY;
      const boxL = parseInt(box.style.left), boxT = parseInt(box.style.top);
      const canvas = document.createElement('canvas');
      canvas.width = 256; canvas.height = 256;
      canvas.getContext('2d').drawImage(img, (boxL - offX) / scale, (boxT - offY) / scale, box.offsetWidth / scale, box.offsetHeight / scale, 0, 0, 256, 256);
      canvas.toBlob(async blob => {
        const form = new FormData();
        form.append('avatar', blob, 'avatar.png');
        try {
          const res = await fetch('/api/v2/account/avatar', { method: 'POST', headers: { 'CSRF-Token': csrf }, body: form });
          if (res.ok) {
            const { avatar } = await res.json();
            avatarPreview.src = avatar + '?t=' + Date.now();
            showToast('<%= req.translations.profilePictureUpdated || "Profile picture updated." %>', 'success');
            close();
          } else showToast('<%= req.translations.failedToUpload || "Failed to upload" %>', 'error');
        } catch (err) { console.error('Avatar upload failed:', err); showToast('<%= req.translations.failedToUpload || "Failed to upload" %>', 'error'); }
      }, 'image/png');
    });
  }
</script>

<% if (allowed) { %>
<script nonce="<%- nonce %>">
(function() {
  const pd = document.getElementById('image-submit-data')?.dataset || {};

  const modal = document.getElementById('imModal');
  if (!modal) return;

  function openModal() {
    if (typeof modal.showModal === 'function') {
      modal.showModal();
    } else {
      // Fallback for browsers without <dialog> support
      modal.classList.remove('opacity-0', 'pointer-events-none');
    }
  }
  function closeModal() {
    if (typeof modal.close === 'function') {
      modal.close();
    } else {
      modal.classList.add('opacity-0', 'pointer-events-none');
    }
  }

  function setMode(mode) {
    const manual = document.getElementById('imManualForm');
    const json = document.getElementById('imJsonSection');
    const url = document.getElementById('imUrlSection');
    const manualBtn = document.getElementById('imModeManual');
    const jsonBtn = document.getElementById('imModeJson');
    const urlBtn = document.getElementById('imModeUrl');
    manual.classList.toggle('hidden', mode !== 'manual');
    json.classList.toggle('hidden', mode !== 'json');
    url.classList.toggle('hidden', mode !== 'url');
    manualBtn.style.background = mode === 'manual' ? 'var(--theme-bg-card)' : 'transparent';
    jsonBtn.style.background = mode === 'json' ? 'var(--theme-bg-card)' : 'transparent';
    urlBtn.style.background = mode === 'url' ? 'var(--theme-bg-card)' : 'transparent';
    manualBtn.style.color = mode === 'manual' ? 'var(--theme-text-strong)' : 'var(--theme-text-muted)';
    jsonBtn.style.color = mode === 'json' ? 'var(--theme-text-strong)' : 'var(--theme-text-muted)';
    urlBtn.style.color = mode === 'url' ? 'var(--theme-text-strong)' : 'var(--theme-text-muted)';
  }

function loader() {
    try {
      return showLoadingPopup(pd.submitting, '');
    } catch {
      return { close: function() {}, updateProgress: function() {} };
    }
  }

  function goToMyImages() {
    closeModal();
    showToast('<%= req.translations.imageSubmitted || "Image submitted. It will appear in the table below once processed." %>', 'success');
  }

  document.getElementById('openSubmitBtn')?.addEventListener('click', openModal);
  document.getElementById('imClose')?.addEventListener('click', closeModal);
  document.getElementById('imManualCancel')?.addEventListener('click', closeModal);
  document.getElementById('imJsonCancel')?.addEventListener('click', closeModal);
  document.getElementById('imUrlCancel')?.addEventListener('click', closeModal);

  // Native <dialog> handles Escape via cancel event; also close on backdrop click.
  modal.addEventListener('cancel', function(e) {
    // Allow default close behavior (Escape key)
  });
  modal.addEventListener('click', function(e) {
    if (e.target === modal) closeModal();
  });

  document.getElementById('imModeManual').addEventListener('click', function() { setMode('manual'); });
  document.getElementById('imModeJson').addEventListener('click', function() { setMode('json'); });
  document.getElementById('imModeUrl').addEventListener('click', function() { setMode('url'); });
  setMode('manual');

  document.getElementById('imManualForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const l = loader();
    try {
      const payload = new FormData(this);
      const data = {};
      for (const [key, value] of payload.entries()) data[key] = value;
      const response = await fetch('/api/v2/account/images', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'CSRF-Token': csrfToken },
        body: JSON.stringify(data),
      });
      const result = await response.json();
      l.close();
      if (result.error && !result.success) {
        showToast(result.error, 'error');
      } else {
        showToast(result.message || '<%= req.translations.submitted || "Submitted" %>', 'success');
        goToMyImages();
      }
    } catch (error) {
      l.close();
      console.error('Failed to submit image:', error);
      showToast(pd.submitError || 'Failed to submit the image.', 'error');
    }
  });

  document.getElementById('imSubmitJsonBtn').addEventListener('click', async function() {
    const raw = document.getElementById('imJsonInput').value;
    if (!raw.trim()) { showToast('<%= req.translations.pasteEggJsonFirst || "Paste an egg JSON first." %>', 'error'); return; }
    let payload;
    try {
      payload = JSON.parse(raw);
    } catch {
      showToast('<%= req.translations.invalidJson || "Invalid JSON." %>', 'error');
      return;
    }
    const l = loader();
    try {
      const response = await fetch('/api/v2/account/images', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'CSRF-Token': csrfToken },
        body: JSON.stringify(payload),
      });
      const result = await response.json();
      l.close();
      if (result.error && !result.success) {
        showToast(result.error, 'error');
      } else {
        showToast(result.message || '<%= req.translations.submitted || "Submitted" %>', 'success');
        goToMyImages();
      }
    } catch (error) {
      l.close();
      console.error('Failed to submit image:', error);
      showToast(pd.submitError || 'Failed to submit the image.', 'error');
    }
  });

  document.getElementById('imSubmitUrlBtn').addEventListener('click', async function() {
    const url = document.getElementById('imUrlInput').value;
    if (!url.trim()) { showToast('<%= req.translations.enterImageUrl || "Please enter an image URL." %>', 'error'); return; }
    const l = loader();
    try {
      const response = await fetch('/api/v2/account/images/import-url', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'CSRF-Token': csrfToken },
        body: JSON.stringify({ url: url.trim() }),
      });
      const result = await response.json();
      l.close();
      if (result.error && !result.success) {
        showToast(result.error, 'error');
      } else {
        showToast(result.message || '<%= req.translations.imported || "Imported" %>', 'success');
        goToMyImages();
      }
    } catch (error) {
      l.close();
      console.error('Failed to import image:', error);
      showToast(pd.submitError || 'Failed to submit the image.', 'error');
    }
  });

  if (window.location.hash === '#submit-image') {
    openModal();
  }
})();
</script>
<% } %>

<% if (allowed) { %>
<script nonce="<%- nonce %>">
(function() {
  // Inline image list: delete handler + empty state sync
  function syncImageEmptyState() {
    const tbody = document.querySelector('#myImagesTable tbody');
    const hasRows = tbody ? !!tbody.querySelector('[data-image-id]') : false;
    const empty = document.getElementById('myImagesEmpty');
    const table = document.getElementById('myImagesTable');
    if (empty) empty.classList.toggle('hidden', hasRows);
    if (table) table.classList.toggle('hidden', !hasRows);
  }

  document.querySelectorAll('[data-delete-image]').forEach(function(btn) {
      btn.addEventListener('click', async function() {
        const id = btn.dataset.deleteImage;
        const msg = '<%= req.translations.deleteImageConfirm || "Delete this image?" %>';
        if (!confirm(msg)) return;
        const row = btn.closest('[data-image-id]');
        try {
          const response = await fetch('/api/v2/account/images/' + id, { method: 'DELETE', headers: { 'CSRF-Token': csrfToken } });
          const data = await response.json();
          if (data.error) {
            showToast(data.error, 'error');
          } else {
            showToast('<%= req.translations.imageDeleted || "Image deleted." %>', 'success');
            window.location.reload();
          }
        } catch (error) {
          console.error('Failed to delete image:', error);
          showToast('<%= req.translations.errorOccurred || "Something went wrong." %>', 'error');
        }
      });
    });
})();
</script>
<% } %>

<%- include('../components/footer') %>
