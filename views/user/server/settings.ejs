<%- include('../../components/header', { title: t('serverSettings') }) %>

<main class="h-screen m-auto" style="color:var(--theme-text)">
  <div class="flex h-full">
    <div class="hidden lg:block w-60 h-full">
      <%- include('../../components/sidebar') %>
    </div>
<%- include('../../components/template') %>
    <div id="page-content" class="flex-1 min-w-0 overflow-y-auto pt-0 pb-12 lg:pb-12">
      <%- include('../../components/ui/breadcrumb', { items: [
        { label: t('dashboard'), href: '/' },
        { label: server.name, href: '/server/' + server.UUID },
        { label: t('settings') }
      ] }) %>
      <!-- Page Header: title + meta left -->
      <div class="px-6 sm:px-8 pt-3 pb-2 sm:pb-4">
        <div class="flex flex-wrap items-start justify-between gap-3">
          <div class="flex-1 min-w-0">
            <%- include('../../components/server-header') %>
            <%- include('../../components/server-meta') %>
          </div>
        </div>
      </div>
      <%- include('../../components/installHeader') %>
      <%- include('../../components/server-template') %>

      <div class="al-page-body space-y-4 mt-6">

        <%- include('../../fragments/user/server/settings-form', { req, server }) %>

        <div class="al-card p-4">
          <h2 class="text-[15px] font-semibold mb-3" style="color:var(--theme-text-strong)"><%= t('serverInformation') %></h2>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('serverId') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)"><%= server.UUID %></p>
            </div>
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('createdAt') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)"><%= new Date(server.createdAt).toLocaleString() %></p>
            </div>
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('nodes') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)"><%= server.node.name %></p>
            </div>
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('images') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)"><%= server.image.name || t('unknown') %></p>
            </div>
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('memory') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)"><%= server.Memory === 0 ? t('unlimited') : (server.Memory >= 1024 ? (server.Memory / 1024).toFixed(1) + ' GB' : server.Memory + ' MB') %></p>
            </div>
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('cpu') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)"><%= server.Cpu === 0 ? t('unlimited') : (server.Cpu / 100).toFixed(1) + ' ' + t('coresLabel') %></p>
            </div>
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('storage') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)"><%= server.Storage === 0 ? t('unlimited') : (server.Storage >= 1024 ? (server.Storage / 1024).toFixed(1) + ' GB' : server.Storage + ' MB') %></p>
            </div>
            <div>
              <h3 class="text-[11px] font-medium uppercase tracking-wide mb-0.5" style="color:var(--theme-text-muted)"><%= t('statusColumn') %></h3>
              <p class="text-[13px]" style="color:var(--theme-text-muted)">
                <span class="inline-flex items-center rounded-md px-1.5 py-0.5 text-[11px] font-medium ring-1 ring-inset"
                  style="<%= server.Suspended ? 'background:var(--theme-warning-bg);color:var(--theme-warning);--tw-ring-color:color-mix(in srgb, var(--theme-warning) 25%, transparent)' : 'background:var(--theme-success-bg);color:var(--theme-success);--tw-ring-color:color-mix(in srgb, var(--theme-success) 25%, transparent)' %>">
                  <%= server.Suspended ? t('suspendedLabel') : t('activeLabel') %>
                </span>
              </p>
            </div>
          </div>
        </div>

        <div class="al-card p-4" style="background:var(--theme-danger-bg);border-color:color-mix(in oklch, var(--theme-danger) 20%, transparent)">
          <h2 class="text-[15px] font-semibold mb-3" style="color:var(--theme-text-strong)"><%= t('dangerZone') %></h2>

          <% if (user && (user.isAdmin || settings?.allowUserDeleteServer)) { %>
          <div class="flex items-center justify-between mb-3">
            <div>
              <h3 class="text-[13px] font-medium" style="color:var(--theme-text-muted)"><%= t('deleteServer') %></h3>
              <p class="text-[11px]" style="color:var(--theme-text-muted)"><%= t('deleteServerDesc') %></p>
            </div>
            <button id="deleteServerButton" type="button" class="al-btn-danger inline-flex items-center gap-1.5">
              <%- icon('trash-2', { class: 'size-3.5', strokeWidth: 1.5 }) %>
              <%= t('delete') %>
            </button>
          </div>
          <% } %>

          <div class="flex items-center justify-between gap-3 mb-3">
            <div>
              <h3 class="text-[13px] font-medium" style="color:var(--theme-text-muted)"><%= t('reinstallServer') %></h3>
              <p class="text-[11px]" style="color:var(--theme-text-muted)"><%= t('reinstallServerDesc') %></p>
            </div>
            <button id="reinstallButton" type="button" class="al-btn-danger inline-flex items-center gap-1.5">
              <%- icon('rotate-ccw', { class: 'size-3.5', strokeWidth: 1.5 }) %>
              <%= t('reinstall') %>
            </button>
          </div>

          <div class="flex items-center justify-between gap-3">
            <div>
              <h3 class="text-[13px] font-medium" style="color:var(--theme-text-muted)"><%= t('reinstallAndDeleteData') %></h3>
              <p class="text-[11px]" style="color:var(--theme-text-muted)"><%= t('reinstallAndDeleteDataDesc') %></p>
            </div>
            <button id="reinstallWipeButton" type="button" class="al-btn-danger inline-flex items-center gap-1.5">
              <%- icon('triangle-alert', { class: 'size-3.5', strokeWidth: 1.5 }) %>
              <%= t('reinstallAndDeleteData') %>
            </button>
          </div>
        </div>

      </div>
    </div>
  </div>
</main>

<%- include('../../components/footer') %>

<script nonce="<%- nonce %>">
  const deleteServerButton = document.getElementById('deleteServerButton');
    if (deleteServerButton) {
      deleteServerButton.addEventListener('click', () => {
        window.modal.confirm({
          title: window.__i18n.deleteServer || "Delete Server",
          body: (window.__i18n.deleteServerConfirm || "This will permanently delete the server and all its data. Type") + '<strong>DELETE</strong> ' + (window.__i18n.toConfirm || "to confirm."),
          danger: true,
          confirmLabel: window.__i18n.deleteServer || "Delete Server",
          prompt: { label: window.__i18n.typeDeleteToConfirm || "Type DELETE to confirm", pattern: /^DELETE$/i, invalidMessage: window.__i18n.mustTypeDelete || "You must type DELETE to confirm." },
          onConfirm: () => {
            const loader = showProgressToast(window.__i18n.deletingServer || "Deleting Server\u2026");
            loader.setStage(window.__i18n.contactingNode || "Contacting node...");
            <% if (user && user.isAdmin) { %>
            const csrfToken = document.querySelector('meta[name="csrf-token"]')?.content || '';
            fetch('/api/v2/admin/servers/<%= server.id %>', {
              method: 'DELETE',
              headers: { 'csrf-token': csrfToken },
            })
              .then(r => {
                loader.close();
                if (r.ok) window.location.href = '/';
                else return r.json().then(d => showToast((d && d.error) || window.__i18n.couldntDeleteServer || "Couldn\u0027t delete the server.", 'error'));
              })
              .catch(() => { loader.close(); showToast(window.__i18n.somethingWentWrong || "Something went wrong.", 'error'); });
            <% } else { %>
            fetch('/api/v2/servers/<%= server.UUID %>', { method: 'DELETE' })
              .then(r => r.json())
              .then(d => { loader.close(); if (d.success) window.location.href = '/'; else showToast((d.error?.message) || window.__i18n.couldntDeleteServer || "Couldn\u0027t delete the server.", 'error'); })
              .catch(() => { loader.close(); showToast(window.__i18n.somethingWentWrong || "Something went wrong.", 'error'); });
            <% } %>
          }
        });
      });
    }

    const reinstallButton = document.getElementById('reinstallButton');
    reinstallButton.addEventListener('click', () => {
      window.modal.confirm({
        title: window.__i18n.confirmReinstallation || "Confirm Reinstallation",
        body: window.__i18n.reinstallConfirm || "Re-run the install scripts. Your files, worlds and settings are kept. Continue?",
        confirmLabel: window.__i18n.reinstall || "Reinstall",
        onConfirm: () => {
          const loader = showProgressToast(window.__i18n.reinstallingServer || "Reinstalling Server\u2026");
          loader.setStage(window.__i18n.preparingReinstall || "Preparing for reinstallation...");
          fetch(`/api/v2/servers/<%= server.UUID %>/reinstall`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ preserveData: true }) })
            .then(r => r.json())
            .then(data => {
              loader.setStage(window.__i18n.reinstallationInitiated || "Reinstallation initiated!");
              setTimeout(() => { loader.close(); if (data.success) showToast(window.__i18n.serverReinstalling || "Server reinstalling. This may take a while.", 'success'); else showToast(window.__i18n.failedToReinstall || "Failed to reinstall server: " + (data.error || window.__i18n.unknownError || "Unknown error"), 'error'); }, 1000);
            })
            .catch(() => { loader.close(); showToast(window.__i18n.somethingWentWrong || "Something went wrong.", 'error'); });
        }
      });
    });

    const reinstallWipeButton = document.getElementById('reinstallWipeButton');
    if (reinstallWipeButton) {
      reinstallWipeButton.addEventListener('click', () => {
        window.modal.confirm({
          title: window.__i18n.reinstallDeleteAllData || "Reinstall & Delete All Data",
          body: (window.__i18n.reinstallDeleteAllDataConfirm || "This wipes the server volume (worlds, configs, files) and reinstalls from scratch. Type") + '<strong>DELETE</strong> ' + (window.__i18n.toConfirm || "to confirm."),
          danger: true, confirmLabel: window.__i18n.continue || "Continue",
          prompt: { label: window.__i18n.typeDeleteToConfirm || "Type DELETE to confirm", pattern: /^DELETE$/i, invalidMessage: window.__i18n.mustTypeDelete || "You must type DELETE to confirm." },
          onConfirm: () => {
                const loader = showProgressToast(window.__i18n.reinstallingServer || "Reinstalling Server\u2026");
            loader.setStage(window.__i18n.preparingReinstall || "Preparing for reinstallation...");
            fetch(`/api/v2/servers/<%= server.UUID %>/reinstall`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ preserveData: false }) })
              .then(r => r.json())
              .then(data => {
                loader.setStage(window.__i18n.reinstallationInitiated || "Reinstallation initiated!");
                setTimeout(() => { loader.close(); if (data.success) showToast(window.__i18n.serverReinstallingAllDataDeleted || "Server reinstalling. All data was deleted.", 'success'); else showToast(window.__i18n.failedToReinstall || "Failed to reinstall server: " + (data.error || window.__i18n.unknownError || "Unknown error"), 'error'); }, 1000);
              })
              .catch(() => { loader.close(); showToast(window.__i18n.somethingWentWrong || "Something went wrong.", 'error'); });
          }
        });
      });
    }
</script>
