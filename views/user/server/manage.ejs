<%- include('../../components/header', { title: t('console'), extraStyles: [assetUrl('/vendor/@xterm/xterm/css/xterm.css')] }) %>
<%- include('../../components/server-features') %>

<style>
    canvas {
        position: absolute;
        left: 0;
        top: 0;
        width: 100% !important;
        height: 100% !important;
        pointer-events: none;
    }

    #terminal {
        height: 420px;
        background: var(--theme-bg);
        overscroll-behavior: contain;
        touch-action: none;
    }

    #terminal .xterm {
        padding: 8px;
        height: 100%;
    }

    #terminal .xterm-viewport {
        background: var(--theme-bg) !important;
        border-radius: 0;
    }

    #terminal .xterm-screen {
        background: var(--theme-bg);
    }

    .console-wrap {
        animation: console-appear 0.35s cubic-bezier(0.16, 1, 0.3, 1) both;
    }

    @keyframes console-appear {
        from { opacity: 0; transform: translateY(6px); }
        to   { opacity: 1; transform: translateY(0); }
    }

    /* Mobile terminal */
    #mobile-terminal {
        height: 300px;
        background: var(--theme-bg);
        overscroll-behavior: contain;
        touch-action: none;
    }

    #mobile-terminal .xterm {
        padding: 6px;
        height: 100%;
    }

    #mobile-terminal .xterm-viewport {
        background: var(--theme-bg) !important;
    }

    #mobile-terminal .xterm-screen {
        background: var(--theme-bg);
    }

    #mobile-terminal-container {
        overscroll-behavior: contain;
        animation: console-appear 0.35s cubic-bezier(0.16, 1, 0.3, 1) both;
    }
</style>

<!-- ═══════════════════════════════════════════════════════════════
     DESKTOP LAYOUT (lg+)
     ═══════════════════════════════════════════════════════════════ -->
<div id="manage-root" data-config="<%= JSON.stringify({
  serverUUID: server.UUID,
  serverSuspended: !!server.Suspended,
  serverMemory: server.Memory || 0,
  serverCpu: server.Cpu || 0,
  serverStorage: server.Storage || 0,
  initialStatus: typeof serverStatus !== 'undefined' ? serverStatus : null,
  wsTokenUrl: '/server/' + server.UUID + '/ws-token',
  logsHistoryUrl: '/server/' + server.UUID + '/logs/history',
  powerBaseUrl: '/server/' + server.UUID + '/power',
  translations: {
    typeACommand: t('typeACommand'),
    confirmRestartServer: t('confirmRestartServer'),
    confirmRestartDesc: t('confirmRestartDesc'),
    restartServer: t('restartServer'),
    confirmStopServer: t('confirmStopServer'),
    confirmStopDesc: t('confirmStopDesc'),
    stopServer: t('stopServer'),
  },
  features: typeof features !== 'undefined' ? features : [],
}) %>">
<main class="hidden lg:block h-screen m-auto" style="color:var(--theme-text)">
  <div class="flex h-full">
    <!-- Sidebar -->
    <div class="hidden lg:block w-60 h-full">
      <%- include('../../components/sidebar') %>
    </div>
<%- include('../../components/template') %>

    <!-- Content -->
    <div id="page-content" class="flex-1 min-w-0 overflow-y-auto pt-0 pb-12 lg:pb-12">
      <%- include('../../components/ui/breadcrumb', { items: [
        { label: t('dashboard'), href: '/' },
        { label: server.name }
      ] }) %>
      <!-- Page Header: title + meta left, buttons right -->
      <div class="px-8 pt-3">
        <div class="flex flex-wrap items-start justify-between gap-3">
          <div class="flex-1 min-w-0">
            <%- include('../../components/server-header') %>
            <%- include('../../components/server-meta') %>
          </div>

          <!-- Control buttons -->
          <div class="flex gap-1.5 shrink-0">
            <button id="startButton" type="button"
              <%= server.Suspended ? 'disabled aria-disabled="true"' : '' %>
              class="al-btn-success rounded-xl px-3 py-1.5 text-[13px] font-medium shadow-sm transition <%= server.Suspended ? 'opacity-50 cursor-not-allowed pointer-events-none' : '' %>">
              <%- icon('play', { class: 'size-3.5 inline-flex mr-1 mb-0.5' }) %>
              <%= t('start') %>
            </button>
            <button id="restartButton" type="button"
              <%= server.Suspended ? 'disabled aria-disabled="true"' : '' %>
              class="al-btn-secondary rounded-xl px-3 py-1.5 text-[13px] font-medium shadow-sm transition <%= server.Suspended ? 'opacity-50 cursor-not-allowed pointer-events-none' : '' %>">
              <%- icon('refresh-cw', { class: 'size-3.5 inline-flex mr-1 mb-0.5' }) %>
              <%= t('restart') %>
            </button>
            <button id="stopButton" type="button"
              <%= server.Suspended ? 'disabled aria-disabled="true"' : '' %>
              class="al-btn-danger rounded-xl px-3 py-1.5 text-[13px] font-medium shadow-sm transition <%= server.Suspended ? 'opacity-50 cursor-not-allowed pointer-events-none' : '' %>">
              <%- icon('square', { class: 'size-3.5 inline-flex mr-1 mb-0.5' }) %>
              <%= t('stop') %>
            </button>
          </div>
        </div>

        <!-- Server Suspended Warning -->
        <% if (server.Suspended) { %>
        <div class="mt-2 border rounded-xl px-3 py-2.5 flex items-center gap-2.5" style="background:color-mix(in srgb, var(--theme-danger) 12%, transparent);border-color:color-mix(in srgb, var(--theme-danger) 20%, transparent)">
          <%- icon('triangle-alert', { class: 'h-4 w-4 shrink-0', style: 'color:var(--theme-danger);' }) %>
          <div>
            <p class="text-[13px] font-medium" style="color:var(--theme-danger);"><%= t('serverSuspended') %></p>
            <p class="text-[11px]" style="color:var(--theme-danger);"><%= t('serverSuspendedDesc') %></p>
          </div>
        </div>
        <% } %>

        <!-- Daemon Offline Warning -->
        <div id="daemonOfflineWarningRow" class="<% if (typeof serverStatus === 'undefined' || !serverStatus.daemonOffline) { %>hidden<% } %> mt-2">
          <div id="daemonOfflineWarning" class="border rounded-xl px-3 py-2.5 flex items-center gap-2.5" style="background:color-mix(in srgb, var(--theme-danger) 12%, transparent);border-color:color-mix(in srgb, var(--theme-danger) 20%, transparent)">
            <%- icon('triangle-alert', { class: 'h-4 w-4 shrink-0', style: 'color:var(--theme-danger);' }) %>
            <div class="flex-1 min-w-0">
              <p class="text-[13px] font-medium" style="color:var(--theme-danger);"><%= (typeof serverStatus !== 'undefined' && serverStatus.error) ? serverStatus.error : t('nodeNotTalking') %></p>
            </div>
            <button type="button" onclick="retryDaemonConnection()" class="al-btn-danger shrink-0 px-2.5 py-1 text-[11px] font-medium rounded-lg transition inline-flex items-center gap-1.5">
              <%- icon('refresh-cw', { class: 'size-3', strokeWidth: 1.5 }) %>
              <%= t('retryConnection') %>
            </button>
          </div>
        </div>
      </div>

      <%- include('../../components/server-template') %>
      <%- include('../../components/installHeader') %>

      <div id="server-page-body" class="flex flex-col lg:flex-row px-8 mt-6">

<!-- Left side: Console -->
<div class="w-full lg:w-2/3 lg:pr-5 flex flex-col min-w-0">
    <div class="flex flex-col rounded-t-xl overflow-hidden shadow-lg flex-1 console-wrap" style="border:1px solid var(--theme-border)">
        <div class="flex items-center gap-2 px-3 py-2 shrink-0" style="background:var(--theme-bg-secondary);border-bottom:1px solid var(--theme-border)">
            <span class="text-[11px] font-medium select-none tracking-wide" style="color:var(--theme-text-muted)"><%= t('console') %></span>
            <span class="flex-1"></span>
            <span id="consoleReadOnlyNotice" class="hidden text-[11px] font-medium" style="color:var(--theme-text-muted)"><%= t('viewOnlyNotice') %></span>
            <button id="loadHistoryBtn" type="button" class="text-[11px] font-medium rounded-md px-2 py-1 transition-colors inline-flex items-center gap-1" style="color:var(--theme-text-muted)" aria-label="<%= t('loadRecentLogsBtn') %>" title="<%= t('loadRecentLogsTitle') %>">
              <%- icon('file-clock', { class: 'size-3', strokeWidth: 1.5 }) %>
              <%= t('loadRecentLogsBtn') %>
            </button>
        </div>
        <div class="flex-1 relative" style="background:var(--theme-bg-card)">
            <div id="terminal"></div>
        </div>
    </div>
    <div class="relative">
        <div class="relative">
            <input id="input" type="text" autocomplete="off" aria-label="<%= t('console') %>" placeholder="<%= t('typeACommand') %>"
                class="w-full px-3 py-2 text-[13px] rounded-b-xl focus:ring-1 focus:border-transparent placeholder:font-medium bg-transparent relative z-[1]"
                style="color:var(--theme-text-strong);border-top:1px solid var(--theme-border)">
            <div id="ghost-text-bg" class="absolute inset-0 px-3 py-2 text-[13px] pointer-events-none rounded-b-xl overflow-hidden z-0" style="border-top:1px solid var(--theme-border);background:var(--theme-bg-secondary)">
                <span id="ghost-typed" class="invisible"></span><span id="ghost-suggestion" style="color:var(--theme-text-muted)"></span>
            </div>
        </div>
    </div>
</div>

        <!-- Right side: Stats Cards -->
        <div class="w-full lg:w-1/3 mt-3 lg:mt-0 space-y-3 flex flex-col">

            <!-- IP Address Card -->
            <div class="stats-card relative flex-1 overflow-hidden rounded-xl p-3 transition-[border-color,box-shadow] duration-150 hover:shadow-md" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:0">
                <div class="relative z-10 flex items-start justify-between gap-2">
                    <div class="min-w-0">
                        <h2 class="text-[11px] font-medium uppercase tracking-wide" style="color:var(--theme-text-muted)"><%= t('addressIP') %></h2>
                        <p id="server-ip-text" class="mt-0.5 text-[13px] font-medium font-mono tracking-tight break-all" style="color:var(--theme-text-strong)"><%= server.node.address %>:<%= (() => { const ports = JSON.parse(server.Ports); const primary = ports.filter(Port => Port.primary); return (primary.length ? primary : ports).slice(-1)[0]?.externalPort || String((primary.length ? primary : ports).slice(-1)[0]?.Port || '').split(':')[0]; })() %></p>
                    </div>
                    <button type="button" id="copy-ip-btn" title="<%= t('copyServerAddress') %>" class="shrink-0 mt-0.5 rounded-lg p-1 transition-colors" style="background:var(--theme-bg-secondary)">
                        <span id="copy-icon"><%- icon('copy', { class: 'h-3 w-3', style: 'color:var(--theme-text-muted)' }) %></span>
                        <span id="check-icon" class="hidden" style="color:var(--theme-success)"><%- icon('check', { class: 'h-3 w-3', strokeWidth: 2.5 }) %></span>
                    </button>
                </div>
            </div>

            <!-- Status Card -->
            <div class="stats-card relative flex-1 overflow-hidden rounded-xl p-3 transition-[border-color,box-shadow] duration-150 hover:shadow-md" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:1">
                <canvas id="statusChart" class="absolute inset-0 w-full h-full"></canvas>
                <div class="relative z-10">
                  <h2 class="text-[11px] font-medium uppercase tracking-wide" style="color:var(--theme-text-muted)"><%= t('Status') %></h2>
                  <p id="status" role="status" aria-live="polite" class="mt-0.5 text-base font-medium tracking-tight leading-snug" style="color:var(--theme-text-strong)">-</p>
                  <p id="status-msg" class="text-[11px] font-medium mt-0.5 leading-snug transition-all duration-300 opacity-0 translate-y-1 pointer-events-none" style="color:var(--theme-text-muted)"></p>
                  <div id="queue-status" class="hidden mt-1.5 text-[11px] leading-snug" style="color:var(--theme-text-muted)">
                    <p id="queue-position" class="font-medium" style="color:var(--theme-warning)"></p>
                    <p id="queue-available" class="mt-0.5"></p>
                    <% if (!server.Suspended) { %>
                    <button type="button" id="cancelQueueBtn" class="mt-1.5 px-2 py-0.5 rounded-lg text-[11px] font-medium border transition hover:bg-[var(--theme-bg-hover)]" style="border-color:var(--theme-border);color:var(--theme-text-muted)"><%= t('cancelStart') %></button>
                    <% } %>
                  </div>
                </div>
              </div>

            <!-- RAM Usage Card -->
            <div class="stats-card relative flex-1 overflow-hidden rounded-xl p-3 transition-[border-color,box-shadow] duration-150 hover:shadow-md" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:2">
                <canvas id="ramChart" class="absolute inset-0 w-full h-full"></canvas>
                <div class="relative z-10">
                    <h2 class="text-[11px] font-medium uppercase tracking-wide" style="color:var(--theme-text-muted)"><%= t('ramUsage') %></h2>
                    <p id="ramUsage" class="mt-0.5 text-base font-medium tracking-tight" style="color:var(--theme-text-strong)">0% (0 MB / 0 MB)</p>
                </div>
            </div>

            <!-- CPU Usage Card -->
            <div class="stats-card relative flex-1 overflow-hidden rounded-xl p-3 transition-[border-color,box-shadow] duration-150 hover:shadow-md" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:3">
                <canvas id="cpuChart" class="absolute inset-0 w-full h-full"></canvas>
                <div class="relative z-10">
                    <h2 class="text-[11px] font-medium uppercase tracking-wide" style="color:var(--theme-text-muted)"><%= t('cpuUsage') %></h2>
                    <p id="cpuUsage" class="mt-0.5 text-base font-medium tracking-tight" style="color:var(--theme-text-strong)">0%</p>
                </div>
            </div>

            <!-- Disk Usage Card -->
            <div class="relative flex-1 overflow-hidden rounded-xl p-3 transition-[border-color,box-shadow] duration-150 hover:shadow-md" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:4">
                <canvas id="diskChart" class="absolute inset-0 w-full h-full hidden"></canvas>
                <div class="relative z-10">
                    <h2 class="text-[11px] font-medium uppercase tracking-wide" style="color:var(--theme-text-muted)"><%= t('diskUsage') %></h2>
                    <p id="diskUsage" class="mt-0.5 text-base font-medium tracking-tight" style="color:var(--theme-text-strong)">-</p>
                </div>
            </div>
        </div>

      </div>
    </div>
  </div>
</main>

<!-- ═══════════════════════════════════════════════════════════════
     MOBILE LAYOUT (< lg) — compact chrome + 2x2 grid + terminal
     ═══════════════════════════════════════════════════════════════ -->
<section id="mobile-manage" class="lg:hidden min-h-screen pt-16" style="color:var(--theme-text)">

  <div class="px-4">
    <!-- Server Header + Meta -->
    <div class="mb-4">
      <div class="flex items-start justify-between gap-3">
        <%- include('../../components/server-header') %>
        <div class="shrink-0 text-right mt-0.5 space-y-0.5">
          <div class="flex items-center justify-end gap-1.5">
            <span class="text-[10px] truncate max-w-[110px]" style="color:var(--theme-text-muted)"><%= server.image.name %></span>
            <%- icon('box', { class: 'h-2.5 w-2.5 shrink-0', style: 'color:var(--theme-text-muted)' }) %>
          </div>
          <div class="flex items-center justify-end gap-1.5">
            <span class="text-[10px] truncate max-w-[110px]" style="color:var(--theme-text-muted)"><%= server.node.name %></span>
            <%- icon('server', { class: 'h-2.5 w-2.5 shrink-0', style: 'color:var(--theme-text-muted)' }) %>
          </div>
          <div class="flex items-center justify-end gap-1.5">
            <span class="text-[10px] font-mono" style="color:var(--theme-text-muted)"><%= server.UUID.split('-')[0] %></span>
            <%- icon('hash', { class: 'h-2.5 w-2.5 shrink-0', style: 'color:var(--theme-text-muted)' }) %>
          </div>
        </div>
      </div>

      <div class="px-0 mt-1">
        <%- include('../../components/loading-popup') %>
      </div>

      <% if (server.Suspended) { %>
      <div class="mt-3 flex items-center gap-2.5 border rounded-xl px-3 py-2.5" style="background:color-mix(in srgb, var(--theme-danger) 12%, transparent);border-color:color-mix(in srgb, var(--theme-danger) 20%, transparent)">
        <%- icon('triangle-alert', { size: 16, class: 'shrink-0', style: 'color:var(--theme-danger);' }) %>
        <div class="min-w-0">
          <p class="text-xs font-medium" style="color:var(--theme-danger);"><%= t('serverSuspended') %></p>
          <p class="text-[11px] truncate" style="color:var(--theme-danger);"><%= t('contactAdminHelp') %></p>
        </div>
      </div>
      <% } %>

      <div id="mobileDaemonOfflineWarning" class="mt-3 flex items-center gap-2.5 border rounded-xl px-3 py-2.5<% if (typeof serverStatus === 'undefined' || !serverStatus.daemonOffline) { %> hidden<% } %>" style="background:color-mix(in srgb, var(--theme-danger) 12%, transparent);border-color:color-mix(in srgb, var(--theme-danger) 20%, transparent)">
        <%- icon('triangle-alert', { size: 16, class: 'shrink-0', style: 'color:var(--theme-danger);' }) %>
        <div class="min-w-0 flex-1">
          <p class="text-xs font-medium" style="color:var(--theme-danger);"><%= t('connectionError') %></p>
          <p class="text-[11px] truncate" style="color:var(--theme-danger);"><%= (typeof serverStatus !== 'undefined' && serverStatus.error) ? serverStatus.error : t('daemonAppearsOffline') %></p>
        </div>
        <button type="button" onclick="retryDaemonConnection()" class="al-btn-danger shrink-0 px-2.5 py-1 text-[11px] font-medium rounded-lg transition inline-flex items-center gap-1.5">
          <%- icon('refresh-cw', { class: 'size-3', strokeWidth: 1.5 }) %>
          <%= t('retry') %>
        </button>
      </div>

      <!-- Mobile Control Buttons -->
      <div class="flex gap-2 mt-3">
        <button id="mobileStartButton" type="button"
          <%= server.Suspended ? 'disabled' : '' %>
          class="al-btn-success flex-1 flex items-center justify-center gap-1.5 rounded-xl px-3 py-2 text-xs font-medium transition <%= server.Suspended ? 'opacity-50 cursor-not-allowed' : '' %>">
          <%- icon('play', { class: 'w-3.5 h-3.5' }) %>
          <%= t('start') %>
        </button>
        <button id="mobileRestartButton" type="button"
          <%= server.Suspended ? 'disabled' : '' %>
          class="al-btn-secondary flex-1 flex items-center justify-center gap-1.5 rounded-xl px-3 py-2 text-xs font-medium transition <%= server.Suspended ? 'opacity-50 cursor-not-allowed' : '' %>">
          <%- icon('refresh-cw', { class: 'w-3.5 h-3.5' }) %>
          <%= t('restart') %>
        </button>
        <button id="mobileStopButton" type="button"
          <%= server.Suspended ? 'disabled' : '' %>
          class="al-btn-danger flex-1 flex items-center justify-center gap-1.5 rounded-xl px-3 py-2 text-xs font-medium transition <%= server.Suspended ? 'opacity-50 cursor-not-allowed' : '' %>">
          <%- icon('square', { class: 'w-3.5 h-3.5' }) %>
          <%= t('stop') %>
        </button>
      </div>
    </div>

    <%- include('../../components/server-template') %>
  </div>

  <%- include('../../components/installHeader') %>

  <div class="px-4 pb-20">
    <!-- IP Address -->
    <div class="mt-3 mb-3">
      <div class="rounded-xl px-3 py-2.5 flex items-center gap-2" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm)">
        <%- icon('globe', { class: 'h-4 w-4 shrink-0', style: 'color:var(--theme-text-muted)' }) %>
        <span class="text-[10px] font-medium shrink-0" style="color:var(--theme-text-muted)"><%= t('addressIP') %>:</span>
        <span class="text-xs font-medium font-mono truncate flex-1" id="mobile-server-ip-text" style="color:var(--theme-text-strong)"><%= server.node.address %>:<%= (() => { const ports = JSON.parse(server.Ports); const primary = ports.filter(Port => Port.primary); return (primary.length ? primary : ports).slice(-1)[0]?.externalPort || String((primary.length ? primary : ports).slice(-1)[0]?.Port || '').split(':')[0]; })() %></span>
        <button type="button" id="mobile-copy-ip-btn" title="<%= t('copyServerAddress') %>" class="shrink-0 rounded-md p-1 transition-colors" style="background:var(--theme-bg-secondary);color:var(--theme-text-muted)">
          <%- icon('copy', { id: 'mobile-copy-icon', class: 'h-3.5 w-3.5', style: 'color:var(--theme-text-muted)', strokeWidth: 2 }) %>
          <%- icon('check', { id: 'mobile-check-icon', class: 'h-3.5 w-3.5 hidden', style: 'color:var(--theme-success)', strokeWidth: 2.5 }) %>
        </button>
      </div>
    </div>

    <!-- Mobile Terminal -->
    <div class="rounded-t-xl overflow-hidden" style="border:1px solid var(--theme-border)">
      <div class="flex items-center px-3 py-2 shrink-0" style="background:var(--theme-bg-secondary);border-bottom:1px solid var(--theme-border)">
        <span class="text-[11px] font-medium select-none tracking-wide" style="color:var(--theme-text-muted)"><%= t('console') %></span>
      </div>
      <div id="mobile-terminal-container" class="w-full relative">
        <div id="mobile-terminal"></div>
      </div>
      <div class="relative" style="border-top:1px solid var(--theme-border)">
        <p id="consoleReadOnlyNoticeMobile" class="hidden text-[11px] px-4 py-1.5 m-0" style="color:var(--theme-text-muted)"><%= t('viewOnlyNotice') %></p>
        <input id="mobile-input" type="text" autocomplete="off" aria-label="<%= t('typeACommand') %>" placeholder="<%= t('typeACommand') %>"
          class="w-full px-4 py-3 text-sm outline-none placeholder:font-medium rounded-b-xl" style="background:var(--theme-bg-card);color:var(--theme-text-strong)">
      </div>
    </div>

    <!-- Mobile Stats Grid (2x2) -->
    <div class="grid grid-cols-2 gap-3 mt-3">
      <!-- Status -->
      <div class="relative overflow-hidden rounded-xl px-3 py-3 min-h-[5rem]" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:0">
        <canvas id="mobileStatusChart"></canvas>
        <div class="relative z-10">
          <p class="text-[10px] font-medium" style="color:var(--theme-text-muted)"><%= t('status') %></p>
          <p id="mobile-status" class="mt-0.5 text-base font-semibold leading-tight" style="color:var(--theme-text-strong)">--</p>
          <p id="mobile-status-log-text" class="text-[10px] truncate hidden" style="color:var(--theme-text-muted)"></p>
        </div>
      </div>
      <!-- RAM -->
      <div class="relative overflow-hidden rounded-xl px-3 py-3 min-h-[5rem]" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:1">
        <canvas id="mobileRamChart"></canvas>
        <div class="relative z-10">
          <p class="text-[10px] font-medium" style="color:var(--theme-text-muted)"><%= t('ramUsage') %></p>
          <p id="mobile-ramUsage" class="mt-0.5 text-xs font-medium leading-tight break-all" style="color:var(--theme-text-strong)">0%</p>
        </div>
      </div>
      <!-- CPU -->
      <div class="relative overflow-hidden rounded-xl px-3 py-3 min-h-[5rem]" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:2">
        <canvas id="mobileCpuChart"></canvas>
        <div class="relative z-10">
          <p class="text-[10px] font-medium" style="color:var(--theme-text-muted)"><%= t('cpuUsage') %></p>
          <p id="mobile-cpuUsage" class="mt-0.5 text-base font-semibold leading-tight" style="color:var(--theme-text-strong)">0%</p>
        </div>
      </div>
      <!-- Disk -->
      <div class="relative overflow-hidden rounded-xl px-3 py-3 min-h-[5rem]" style="background:var(--theme-bg-card);border:1px solid var(--theme-border);box-shadow:var(--theme-shadow-sm);--i:3">
        <canvas id="mobileDiskChart" class="hidden"></canvas>
        <div class="relative z-10">
          <p class="text-[10px] font-medium" style="color:var(--theme-text-muted)"><%= t('diskUsage') %></p>
          <p id="mobile-diskUsage" class="mt-0.5 text-xs font-medium leading-tight break-all" style="color:var(--theme-text-strong)">--</p>
        </div>
      </div>
    </div>
  </div>
</section>

</div><!-- /manage-root -->

<%- include('../../components/footer') %>

<script nonce="<%- nonce %>">
function retryDaemonConnection() { window.location.reload(); }
(async function () {
  const root = document.getElementById('manage-root');
  if (!root) return;
  try {
    const config = JSON.parse(root.dataset.config || '{}');
    const { mount } = await import('<%= assetUrl('/javascript/islands/server-console.js') %>');
    const controller = await mount(root, config);
    window._manageCleanup = controller && controller.teardown;
  } catch (error) {
    console.error('Failed to initialize the server console.', error);
    const status = root.querySelector('#status');
    if (status) status.textContent = '<%= req.translations.consoleUnavailable || "Console unavailable" %>';
    const message = root.querySelector('#status-msg');
    if (message) {
      message.textContent = '<%= req.translations.refreshPageToRetry || "Refresh the page to retry. If this continues, check the browser console." %>';
      message.classList.remove('opacity-0', 'translate-y-1', 'pointer-events-none');
    }
    if (window.showToast) window.showToast('<%= req.translations.consoleCouldNotLoad || "Console could not load. Refresh the page to retry." %>', 'error');
  }
})();
</script>
