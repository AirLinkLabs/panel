<%- include('../components/header', { title: t('credits') }) %>
<script nonce="<%- nonce %>">
  const CREDITS_CONFIG = {
    contributorsUrl: '<%= GITHUB_CONTRIBUTORS_URL %>',
    repoUrl: '<%= GITHUB_REPO_URL %>',
    discordUrl: '<%= DISCORD_INVITE_URL %>',
    websiteUrl: '<%= COMPANY_WEBSITE_URL %>',
    docsUrl: '<%= DOCS_QUICKSTART_URL %>',
    licenseUrl: '<%= GITHUB_LICENSE_URL %>',
    avatarUrl: function(login) { return 'https://github.com/' + encodeURIComponent(login) + '.png'; },
    avatarFallback: '<%= GITHUB_AVATAR_FALLBACK %>',
  };
</script>

<style>
  .credits-bg {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }
  .credits-bg canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity 0.8s ease;
  }
  .credits-bg.fading-out canvas {
    opacity: 0 !important;
    transition: opacity 0.3s ease;
  }
</style>

<!-- Easter egg background -->
<div class="credits-bg" aria-hidden="true">
  <canvas id="credits-canvas"></canvas>
</div>

<main class="h-screen m-auto relative z-10">
  <div class="flex h-screen">
    <div class="hidden lg:block w-60 h-full shrink-0"><%- include('../components/sidebar') %></div>
<%- include('../components/template') %>
    <div id="page-content" class="flex-1 overflow-y-auto pt-0 pb-12">
      <%- include('../components/ui/breadcrumb', { items: [
        { label: t('credits') }
      ] }) %>

      <div class="px-8 pt-5 mb-8">
        <div class="flex items-center gap-3 mb-1">
          <h1 class="text-xl font-semibold tracking-tight" style="color:var(--theme-text-strong);"><%= t('credits') %></h1>
          <span class="text-[10px] font-mono px-2 py-0.5 rounded-md" style="background:var(--theme-bg-secondary); border:1px solid var(--theme-border); color:var(--theme-text-muted);">v<%= version %></span>
        </div>
        <p class="text-sm" style="color:var(--theme-text-muted);"><%= t('creditsPageDesc') %></p>
      </div>

      <div class="px-6 sm:px-8 space-y-8 max-w-5xl">

        <!-- Project leads — horizontal row on desktop -->
        <section>
          <h2 class="text-xs font-semibold uppercase tracking-widest mb-4" style="color:var(--theme-text-muted);"><%= t('projectLeads') %></h2>
          <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
            <% const leads = [
              { login: 'bthavanish', role: 'Current maintainer' },
              { login: 'privt00',   role: 'Project lead' },
              { login: 'achul123',  role: 'Core developer' },
            ]; %>
            <% leads.forEach(function(lead) { %>
            <div class="al-card flex items-center gap-4 px-5 py-4">
              <img src="<%= GITHUB_AVATAR_URL(lead.login) %>?size=80"
                   alt="<%= lead.login %>"
                   loading="lazy"
                   class="h-12 w-12 rounded-xl object-cover shrink-0"
                   style="border-color:var(--theme-border)"
                   onerror="this.src='<%= GITHUB_AVATAR_FALLBACK %>'">
              <div class="min-w-0 flex-1">
                <p class="text-sm font-semibold truncate" style="color:var(--theme-text-strong);"><%= lead.login %></p>
                <p class="text-xs mt-0.5" style="color:var(--theme-text-muted);"><%= lead.role %></p>
                <a href="https://github.com/<%= lead.login %>" target="_blank" rel="noopener"
                   class="inline-flex items-center gap-1 text-xs mt-1.5 transition-colors" style="color:var(--theme-text-muted);">
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-3 h-3">
                    <path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12Z"/>
                  </svg>
                  GitHub
                </a>
              </div>
            </div>
            <% }); %>
          </div>
        </section>

        <!-- Contributors -->
        <section>
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-xs font-semibold uppercase tracking-widest" style="color:var(--theme-text-muted);"><%= t('contributors') %></h2>
            <span id="contrib-cache-label" class="text-xs" style="color:var(--theme-text-muted);"></span>
          </div>
          <div id="contributors-grid" class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
            <div class="col-span-full flex items-center gap-2 py-2">
              <div class="h-4 w-4 border-2 rounded-full animate-spin" style="border-color:var(--theme-border);border-top-color:var(--theme-text)"></div>
              <span class="text-sm" style="color:var(--theme-text-muted);"><%= t('loadingContributors') %></span>
            </div>
          </div>
        </section>

        <!-- Links — full horizontal row -->
        <section>
          <h2 class="text-xs font-semibold uppercase tracking-widest mb-4" style="color:var(--theme-text-muted);"><%= t('links') %></h2>
          <div class="grid grid-cols-2 sm:grid-cols-4 gap-3">

            <a href="<%= GITHUB_REPO_URL %>" target="_blank" rel="noopener"
              class="al-card flex items-center gap-3 px-4 py-3.5 hover:border-[var(--theme-border-accent)] transition-colors">
              <span class="w-8 h-8 flex items-center justify-center rounded-lg shrink-0" style="background:var(--theme-bg-secondary);">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-4 h-4" style="color:var(--theme-text-muted);"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12Z"/></svg>
              </span>
              <div><p class="text-sm font-medium" style="color:var(--theme-text-strong);">GitHub</p><p class="text-xs" style="color:var(--theme-text-muted);"><%= t('sourceCode') %></p></div>
            </a>

            <a href="<%= DISCORD_INVITE_URL %>" target="_blank" rel="noopener"
              class="al-card flex items-center gap-3 px-4 py-3.5 hover:border-[var(--theme-border-accent)] transition-colors">
              <span class="w-8 h-8 flex items-center justify-center rounded-lg shrink-0" style="background:var(--theme-bg-secondary);">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-4 h-4" style="color:var(--theme-text-muted);"><path d="M20.317 4.37a19.791 19.791 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057c.002.022.015.043.03.056a19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03ZM8.02 15.33c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.956-2.419 2.157-2.419 1.21 0 2.176 1.096 2.157 2.42 0 1.333-.956 2.418-2.157 2.418Zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.419 2.157-2.419 1.21 0 2.176 1.096 2.157 2.42 0 1.333-.946 2.418-2.157 2.418Z"/></svg>
              </span>
              <div><p class="text-sm font-medium" style="color:var(--theme-text-strong);">Discord</p><p class="text-xs" style="color:var(--theme-text-muted);"><%= t('community') %></p></div>
            </a>

            <a href="<%= COMPANY_WEBSITE_URL %>" target="_blank" rel="noopener"
              class="al-card flex items-center gap-3 px-4 py-3.5 hover:border-[var(--theme-border-accent)] transition-colors">
              <span class="w-8 h-8 flex items-center justify-center rounded-lg shrink-0" style="background:var(--theme-bg-secondary);">
                <%- icon('globe', { class: 'w-4 h-4', style: 'color:var(--theme-text-muted);', strokeWidth: 1.5 }) %>
              </span>
              <div><p class="text-sm font-medium" style="color:var(--theme-text-strong);"><%= t('website') %></p><p class="text-xs" style="color:var(--theme-text-muted);">airlinklabs.xyz</p></div>
            </a>

            <a href="<%= DOCS_QUICKSTART_URL %>" target="_blank" rel="noopener"
              class="al-card flex items-center gap-3 px-4 py-3.5 hover:border-[var(--theme-border-accent)] transition-colors">
              <span class="w-8 h-8 flex items-center justify-center rounded-lg shrink-0" style="background:var(--theme-bg-secondary);">
                <%- icon('book-open', { class: 'w-4 h-4', style: 'color:var(--theme-text-muted);', strokeWidth: 1.5 }) %>
              </span>
              <div><p class="text-sm font-medium" style="color:var(--theme-text-strong);"><%= t('documentation') %></p><p class="text-xs" style="color:var(--theme-text-muted);"><%= t('documentation') %></p></div>
            </a>

          </div>
        </section>

        <!-- License -->
        <section class="pb-4">
          <h2 class="text-xs font-semibold uppercase tracking-widest mb-4" style="color:var(--theme-text-muted);"><%= t('license') %></h2>
          <div class="al-card flex items-start gap-4 px-5 py-4">
            <span class="shrink-0 mt-0.5 w-8 h-8 flex items-center justify-center rounded-lg" style="background:var(--theme-bg-secondary);">
              <%- icon('file-text', { class: 'w-4 h-4', style: 'color:var(--theme-text-muted);', strokeWidth: 1.5 }) %>
            </span>
            <div>
              <p class="text-sm font-medium mb-1" style="color:var(--theme-text-strong);"><%= t('mitLicense') %></p>
              <p class="text-xs leading-relaxed" style="color:var(--theme-text-muted);"><%= t('mitLicenseDesc') %></p>
              <a href="<%= GITHUB_LICENSE_URL %>" target="_blank" rel="noopener" class="inline-block mt-2 text-xs underline transition-colors" style="color:var(--theme-text-muted);"><%= t('viewFullLicense') %></a>
            </div>
          </div>
        </section>

      </div>
    </div>
  </div>
</main>


<%- include('../components/footer') %>

<script nonce="<%- nonce %>">
(function () {
  // Fill the -_- background
  // Fill -_- background — generate once, set once, CSS handles the animation
  (function () {
// ── -_- infinite canvas scroller ─────────────────────────────────────
    (function () {
      const canvas  = document.getElementById('credits-canvas');
      if (!canvas) return;
      const ctx     = canvas.getContext('2d');
      const TILE    = '-_-  ';
      const ANGLE   = -12 * Math.PI / 180;
      const SPEED   = 28;
      const FONT_SZ = 17;
      const LINE_H  = Math.round(FONT_SZ * 2.6);
      let offset  = 0;
      let last    = null;
      let raf     = null;
      let running = false;

      function isDark() {
        return document.documentElement.classList.contains('dark');
      }

      function resize() {
        canvas.width  = window.innerWidth;
        canvas.height = window.innerHeight;
      }

      function draw(ts) {
        if (!running) return;
        raf = requestAnimationFrame(draw);

        // Advance offset
        if (last !== null) offset += SPEED * (ts - last) / 1000;
        last = ts;
        // Wrap offset to LINE_H so it never grows unbounded
        const wrapped = offset % LINE_H;

        const W = canvas.width;
        const H = canvas.height;

        ctx.clearRect(0, 0, W, H);
        ctx.save();

        // Move origin to centre, rotate, then shift so the pattern tiles
        ctx.translate(W / 2, H / 2);
        ctx.rotate(ANGLE);

        // How wide/tall the rotated slab needs to be to cover every corner
        const D = Math.ceil(Math.sqrt(W * W + H * H));

        ctx.font = 'bold ' + FONT_SZ + 'px monospace';
        ctx.letterSpacing = '0.16em';
        ctx.fillStyle = isDark()
          ? 'rgba(255,255,255,0.15)'
          : 'rgba(0,0,0,0.18)';

        // How many tiles fill one row at this diagonal width
        const tileW   = ctx.measureText(TILE).width;
        const tilesPR = Math.ceil((D * 2) / tileW) + 2;
        const row     = TILE.repeat(tilesPR);

        // Draw rows from top of slab to bottom, shifted by scroll offset
        const startY = -D - LINE_H + wrapped;
        for (let y = startY; y < D + LINE_H; y += LINE_H) {
          ctx.fillText(row, -D, y);
        }

        ctx.restore();
      }

      function start() {
        if (running) return;
        running = true;
        last    = null;
        canvas.style.opacity = '1';
        raf = requestAnimationFrame(draw);
      }

      function stop() {
        running = false;
        if (raf) { cancelAnimationFrame(raf); raf = null; }
      }

      resize();
      window.addEventListener('resize', resize);

      // Start immediately — no overlay to wait for
      start();

      // Fade out and stop on navigation
      document.addEventListener('click', function (e) {
        const a = e.target && e.target.closest && e.target.closest('a[href]');
        if (!a) return;
        const href = a.getAttribute('href');
        if (!href || href === '#' || href.startsWith('#') || a.target === '_blank') return;
        const bg = document.querySelector('.credits-bg');
        if (bg) {
          bg.classList.add('fading-out');
          setTimeout(stop, 350);
        }
      }, true);
    })();
  })();

  // Contributors cache
  const CACHE_KEY    = 'airlink_contributors_v2';
  const CACHE_TTL_MS = 6 * 60 * 60 * 1000;
  const LEADS        = ['bthavanish', 'privt00', 'achul123'];

  function showCacheLabel(ts) {
    const el = document.getElementById('contrib-cache-label');
    if (!el) return;
    const mins = Math.round((Date.now() - ts) / 60000);
    el.textContent = mins < 2 ? window.__i18n.justUpdated || "Just updated" : window.__i18n.updatedAgo || "Updated" + ' + mins + 'm ' + window.__i18n.ago || "ago";
  }

  function renderContributors(list) {
    const grid = document.getElementById('contributors-grid');
    if (!grid) return;
    const others = list.filter(function (c) {
      return !LEADS.includes(c.login) && c.type !== 'Bot';
    });
    if (!others.length) {
      grid.innerHTML = '<p class="col-span-full text-sm" style="color:var(--theme-text-muted);">window.__i18n.noOtherContributors || "No other contributors found."</p>';
      return;
    }
    grid.innerHTML = others.map(function (c) {
      const safeLogin = escHtml(c.login);
      const safeUrl = escHtml(c.html_url);
      const safeAvatar = escHtml(c.avatar_url);
        return '<a href="' + safeUrl + '" target="_blank" rel="noopener"' +
        ' class="al-card flex items-center gap-3 px-3 py-3 hover:border-[var(--theme-border-accent)] transition-colors min-w-0">' +
        '<img loading="lazy" src="' + safeAvatar + '&s=48" alt="' + safeLogin + '" class="h-8 w-8 rounded-lg border border-neutral-200 dark:border-white/10 object-cover shrink-0" onerror="this.src=\'' + CREDITS_CONFIG.avatarFallback + '\'">' +
        '<div class="min-w-0 flex-1"><p class="text-xs font-medium truncate" style="color:var(--theme-text-strong);">' + safeLogin + '</p>' +
        '<p class="text-[10px]" style="color:var(--theme-text-muted);">' + c.contributions + ' ' + window.__i18n.commit || "commit" + (c.contributions !== 1 ? 's' : '') + '</p></div></a>';
    }).join('');
  }

  // Try cache first, then fetch
  let cached = null;
  try {
    const raw = localStorage.getItem(CACHE_KEY);
    if (raw) {
      const parsed = JSON.parse(raw);
      if (Date.now() - parsed.ts < CACHE_TTL_MS) {
        cached = parsed;
      }
    }
  } catch (_) { /* localStorage unavailable */ }

  if (cached) {
    showCacheLabel(cached.ts);
    renderContributors(cached.data);
  } else {
    fetch(CREDITS_CONFIG.contributorsUrl)
      .then(function (r) { return r.ok ? r.json() : Promise.reject(); })
      .then(function (data) {
        try { localStorage.setItem(CACHE_KEY, JSON.stringify({ ts: Date.now(), data: data })); } catch (_) { /* quota exceeded */ }
        showCacheLabel(Date.now());
        renderContributors(data);
      })
      .catch(function () {
        const grid = document.getElementById('contributors-grid');
        if (grid) grid.innerHTML = '<p class="col-span-full text-sm" style="color:var(--theme-text-muted);">window.__i18n.failedToLoadContributors || "Could not load contributors. GitHub may be rate limiting — try again later."</p>';
      });
  }
})();
</script>
